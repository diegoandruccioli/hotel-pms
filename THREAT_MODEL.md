# THREAT MODEL — Hotel PMS

**Metodologia**: STRIDE (Spoofing, Tampering, Repudiation, Information Disclosure, Denial of Service, Elevation of Privilege)  
**Branch di riferimento**: `feature/secure-coding-hardening`  
**Data ultima revisione**: 2026-08-22  
**Autore**: Diego Andruccioli

---

## 1. Scope & Assets da Proteggere

| Asset | Classificazione | Servizio |
|-------|----------------|---------|
| Credenziali utente (password hash) | CRITICO | auth-service |
| Token JWT (access + refresh) | CRITICO | auth-service, api-gateway |
| Segreti di configurazione (HMAC key, JWT secret) | CRITICO | config-service |
| Dati ospiti (PII: nome, documento, nazionalità) | ALTO | guest-service |
| Dati di prenotazione | ALTO | frontdesk-service (ex reservation-service, consolidato ADR-001) |
| Report Alloggiati (dati PS) | ALTO | frontdesk-service (ex stay-service, consolidato ADR-001) |
| Dati di fatturazione | ALTO | billing-service |
| Ordini F&B collegati a camera | MEDIO | fb-service |
| Disponibilità camere | MEDIO | frontdesk-service (ex inventory-service, consolidato ADR-001) |
| Notifiche email transazionali (indirizzo ospite in corpo/subject) | MEDIO | notification-service |
| Backup del database (dump/WAL, locale + off-site) | CRITICO | postgres (pgBackRest) |

---

## 2. Architettura & Data Flow

```
[Browser/Client]
      │  HTTPS (cookie httpOnly)
      ▼
[API Gateway :8080]  ← JWT validation, rate limiting (Redis), CORS, headers HTTP
      │  X-Auth-User / X-Auth-Role / X-Auth-Hotel / X-Internal-Signature
      ├──► [auth-service :8087]         ← login, register, refresh token
      ├──► [guest-service :8083]        ← CRUD ospiti
      ├──► [frontdesk-service :8081]    ← rooms, prenotazioni, check-in/out, alloggiati (ADR-001: ex inventory+reservation+stay-service)
      │        │
      │        └──► [notification-service :8088]  ← email transazionali (nessuna route gateway, solo rete interna)
      ├──► [billing-service :8085]      ← fatture, pagamenti
      └──► [fb-service :8086]           ← POS F&B
             │
             └── (direct, no gateway) ← [config-service :8888]
                                          [PostgreSQL x5]
                                          [Redis :6379 — rate limit, refresh-token blacklist, nonce anti-replay]
                                          [Zipkin :9411]
```

---

## 3. Trust Boundaries

| Boundary | Descrizione | Rischio se violata |
|----------|-------------|-------------------|
| Internet → API Gateway | Ingresso pubblico non autenticato | Accesso non autorizzato a tutti i servizi |
| API Gateway → Services | Header interni iniettati dal gateway | Bypass autenticazione se servizi esposti direttamente |
| Services → Config Service | Lettura segreti applicativi | Esposizione JWT secret, HMAC key |
| Service → Service (Feign) | Comunicazione inter-servizio | Falsificazione identità, data tampering |
| Application → Database | Query ORM | SQL Injection se non parametrizzato |

---

## 4. Threat Enumeration per Servizio (STRIDE)

**Legenda stato**: 🔴 APERTO | 🟡 DA ANALIZZARE | 🔄 IN CORSO | ✅ RISOLTO

### 4.1 auth-service

| ID | Categoria STRIDE | Threat | Impatto | Probabilità | Stato |
|----|-----------------|--------|---------|-------------|-------|
| T-AUTH-01 | Information Disclosure | User Enumeration: messaggi di errore distinti per "utente non trovato" vs "password errata" | ALTO | ALTA | ✅ RISOLTO |
| T-AUTH-02 | Denial of Service | Brute Force: nessun rate limiting sul login a livello di servizio | CRITICO | ALTA | ✅ RISOLTO |
| T-AUTH-03 | Spoofing | Password debole: algoritmo di hashing da verificare (bcrypt vs Argon2) + policy complessità insufficiente | CRITICO | MEDIA | ✅ RISOLTO |
| T-AUTH-04 | Elevation of Privilege | Refresh Token: assenza di meccanismo di revoca (token rotation) | ALTO | MEDIA | ✅ RISOLTO |
| T-AUTH-05 | Repudiation | Assenza di audit log per eventi di autenticazione (login, logout, failed attempts) | MEDIO | ALTA | ✅ RISOLTO |
| T-AUTH-06 | Elevation of Privilege | `mustChangePassword` (password provvisoria/reset admin) applicato solo come redirect frontend: navigazione diretta a un'altra URL bypassa il redirect e le chiamate API dirette funzionano normalmente con le credenziali provvisorie | MEDIO | ALTA | ✅ RISOLTO |
| T-AUTH-07 | Denial of Service | Riattivare un utente disattivato è impossibile: `activateUser` usa `findById`, filtrato da `@SQLRestriction("active = true")` a livello entità — non trova mai un utente `active=false`, 404 sempre. Nessun workaround da UI, unico rimedio è intervento diretto su DB | CRITICO | ALTA | ✅ RISOLTO |
| T-AUTH-08 | Denial of Service | Il refresh silenzioso (T-AUTH-04) esclude esplicitamente `/auth/me` dal retry-dopo-refresh per "prevenire loop" — ma `/me` è read-only, un refresh-poi-retry non può causare loop. Effetto: un F5/link diretto dopo 15 minuti (scadenza access token) forza logout completo anche con un refresh token pienamente valido per altri 7 giorni — nega al legittimo proprietario la sessione persistente che il meccanismo di refresh esiste apposta per garantire | ALTO | ALTA | ✅ RISOLTO |
| T-AUTH-09 | Denial of Service | Il cambio password volontario (non da flusso `mustChangePassword`) forzava un logout completo lato frontend anche se il backend (T-AUTH-04 residuo) emette apposta un nuovo token pair proprio per preservare la sessione — l'utente doveva ridigitare da capo la password appena impostata | MEDIO | ALTA | ✅ RISOLTO |
| T-AUTH-10 | Elevation of Privilege | `UserManagementController` usa `@PreAuthorize`, ma auth-service non aveva mai gestito `AccessDeniedException` nel proprio `GlobalExceptionHandler` — un diniego RBAC cadeva nel catch-all generico e tornava 500 invece di 403. Non exploitable da solo (l'accesso veniva comunque negato, `@PreAuthorize` ha sempre funzionato), ma un client/monitoring non può distinguere un diniego di autorizzazione da un guasto server reale, e nasconde il segnale dietro un errore generico invece del codice HTTP standard | BASSO | BASSA | ✅ RISOLTO |
| T-AUTH-11 | Elevation of Privilege | `POST /api/v1/auth/register` era un endpoint pubblico (nessuna autenticazione) che accettava `role` e `hotelId` direttamente dal body client — un attaccante non autenticato poteva auto-registrarsi come `ADMIN`/`OWNER` su qualunque `hotelId` a scelta, senza alcuna verifica. Trovato dall'audit di sicurezza a 10 sotto-agenti paralleli (`security-audit/`, 2026-08) | CRITICO | ALTA | ✅ RISOLTO |
| T-AUTH-12 | Denial of Service | Account lockout (T-AUTH-02) legato solo allo username: un attaccante poteva bloccare permanentemente l'accesso al vero proprietario di un account noto (es. `admin`) inviando ripetutamente password errate, senza bisogno di credenziali valide. Rilevato dallo stesso audit a 10 agenti | ALTO | ALTA | ✅ RISOLTO — mitigazione (lockout su username+IP) più fix GAP-17 sulla topologia nginx→gateway |
| T-AUTH-13 | Information Disclosure | User enumeration su due canali distinti: (1) `AccountLockedException` restituiva un codice/corpo diverso da `BadCredentialsException`, confermando l'esistenza dell'username una volta raggiunto il lockout; (2) uno username inesistente restituiva 401 immediatamente (nessun controllo password), mentre uno username esistente con password errata pagava il costo pieno dell'hashing Argon2id — un attaccante poteva enumerare username misurando la latenza della risposta. Rilevato dallo stesso audit a 10 agenti | MEDIO | ALTA | ✅ RISOLTO |

### 4.2 api-gateway

| ID | Categoria STRIDE | Threat | Impatto | Probabilità | Stato |
|----|-----------------|--------|---------|-------------|-------|
| T-GW-01 | Tampering | Header X-Auth-User/Role iniettabili dal client se non rimossi in ingresso | CRITICO | MEDIA | ✅ RISOLTO |
| T-GW-07 | Tampering | HMAC copre solo username:role — X-Auth-Hotel modificabile senza invalidare la firma (header-tampering sul tenant context) | ALTO | BASSA | ✅ RISOLTO |
| T-GW-02 | Denial of Service | Rate limiting insufficiente o bypassabile per certi endpoint | ALTO | MEDIA | ✅ RISOLTO |
| T-GW-03 | Information Disclosure | Assenza header sicurezza HTTP (CSP, HSTS, X-Frame-Options, X-Content-Type-Options) | MEDIO | ALTA | ✅ RISOLTO |
| T-GW-04 | Spoofing | CORS misconfiguration: origini non ristrette | ALTO | MEDIA | ✅ RISOLTO |
| T-GW-05 | Tampering | CSRF: mancanza protezione esplicita per operazioni mutanti via cookie | ALTO | MEDIA | ✅ RISOLTO |
| T-GW-06 | Elevation of Privilege | X-Auth-Hotel non propagato: UserAccount senza hotel_id, JWT senza claim hotelId, AuthenticationFilter non inietta X-Auth-Hotel → isolamento multi-tenant IDOR non funzionante end-to-end | CRITICO | ALTA | ✅ RISOLTO |
| T-GW-08 | Spoofing | HMAC interno (X-Internal-Signature) firma solo username:role:hotelId, senza timestamp/nonce: un set di header catturato (es. accesso alla rete Docker interna) resta una credenziale valida indefinitamente — nessuna protezione anti-replay | ALTO | BASSA | ✅ RISOLTO |
| T-GW-09 | Elevation of Privilege | Il endpoint di scrittura amministrativa POST /api/v1/rate-calendar/bulk-apply (introdotto nella stessa feature) mancava della seconda barriera di difesa a livello gateway (WRITE_RESTRICTED_PREFIXES) che la sua risorsa gemella /api/v1/room-types già ha — non exploitable da solo (RateCalendarController già applica @PreAuthorize hasAnyRole ADMIN/OWNER a livello servizio), ma rompeva il pattern a due livelli usato per ogni altra risorsa admin-only | BASSO | BASSA | ✅ RISOLTO |
| T-GW-10 | Denial of Service | `RateLimiterConfig` si fidava del valore leftmost dell'header client-supplied `X-Forwarded-For` per il bucketing del rate limit — un attaccante poteva ruotare valori XFF falsificati per ottenere bucket illimitati e bypassare completamente il rate limiting sul login e su ogni altra rotta. Rilevato dall'audit a 10 agenti | ALTO | ALTA | ✅ RISOLTO — la fonte spoofabile resta rimossa (mai X-Forwarded-For); GAP-17 chiude anche il trust boundary nginx→gateway che l'aveva vanificata |
| T-GW-11 | Elevation of Privilege | **Trovato in security review prima del merge, non solo modellato**: il nuovo endpoint SSE `GET /api/v1/events/stream` (sincronizzazione realtime stato camere) era raggiunto tramite una route self-referenziale (`uri: forward:/api/v1/events/stream`) che mappava `EventStreamController` sulla **stessa** path pubblica su cui la route matcha. `DispatcherHandler` interroga i bean `HandlerMapping` in ordine crescente: `RequestMappingHandlerMapping` di WebFlux (order 0, risolve `@GetMapping`) viene consultato *prima* di `RoutePredicateHandlerMapping` di Gateway (order 1) — un client che chiamava direttamente `GET /api/v1/events/stream` veniva risolto dal primo, mai dal secondo, saltando per intero la catena di filtri della route: `AuthenticationFilter` non veniva mai eseguito. Un chiamante non autenticato (nessun cookie JWT) poteva inviare un header `X-Auth-Hotel` arbitrario e ricevere lo stream realtime di un hotel qualunque — bypass completo di autenticazione più IDOR cross-tenant sul canale, non solo un rischio residuo | CRITICO | ALTA | ✅ RISOLTO — separazione strutturale dei due path: `EventStreamController` ora mappato su `/local/events/stream` (path interno, mai esposto), la route pubblica `/api/v1/events/stream` continua a puntare lì solo tramite `uri: forward:/local/events/stream`; poiché nessun `@GetMapping` risolve più la path pubblica, `RequestMappingHandlerMapping` non può più intercettarla e solo `RoutePredicateHandlerMapping` (quindi `AuthenticationFilter`) la risolve. Nuovo `EventStreamControllerMappingTest` asserisce che le due path non coincidano mai (guardia di regressione). L'hotelId resta comunque letto esclusivamente dall'header `X-Auth-Hotel` iniettato da `AuthenticationFilter` dal claim JWT verificato, mai da input diretto del client |
| T-GW-12 | Spoofing | Nuovo endpoint interno `POST /internal/events/notify` (frontdesk-service → gateway): primo caso in cui il gateway *riceve*, e non solo produce, una chiamata interna firmata HMAC. `InternalAuthFilter` (`internal-auth-lib`), il verificatore condiviso da tutti gli altri 6 servizi, è servlet-only (`OncePerRequestFilter`) e non riusabile su questo gateway reattivo/WebFlux — un verificatore reimplementato in modo indipendente rischia di divergere dall'algoritmo condiviso (finestra di replay, confronto a tempo costante, claim del nonce) | ALTO | BASSA | ✅ RISOLTO — `InternalEventsAuthFilter` (nuovo `WebFilter` reattivo) replica byte-per-byte lo stesso algoritmo di `InternalAuthFilter`/`AuthenticationFilter.computeHmac` (payload `user:role:hotelId:timestamp:nonce`, finestra ±60s, confronto `MessageDigest.isEqual`, nonce store con TTL 120s — reattivo via `ReactiveStringRedisTemplate.setIfAbsent`, dato che il gateway ha solo lo stack Redis reattivo), verificato da una matrice di test a specchio di `InternalAuthFilterTest` |

### 4.3 guest-service

| ID | Categoria STRIDE | Threat | Impatto | Probabilità | Stato |
|----|-----------------|--------|---------|-------------|-------|
| T-GST-01 | Elevation of Privilege | IDOR: un RECEPTIONIST potrebbe accedere a ospiti di altri hotel via UUID prevedibile | CRITICO | MEDIA | ✅ RISOLTO |
| T-GST-02 | Injection | Input validation: campi testo libero (nome, note) non sanitizzati | ALTO | MEDIA | ✅ RISOLTO |
| T-GST-03 | Information Disclosure | Esposizione PII senza controllo hotel_id (multi-tenant data leak) | CRITICO | ALTA | ✅ RISOLTO |
| T-GST-04 | Tampering | Mass Assignment: DTO potrebbe accettare campi non previsti tramite JSON | MEDIO | MEDIA | ✅ RISOLTO |
| T-GST-05 | Information Disclosure | GDPR Data Retention: PII ospite (nome, data nascita, documento, numero documento) conservata a tempo indeterminato senza retention policy; soft-delete non soddisfa diritto all'oblio (Art. 17 GDPR); nessun campo consenso su entità Guest | ALTO | MEDIA | ✅ RISOLTO |
| T-GST-06 | Tampering | Regressione della guardia legale T-GST-05: `DELETE /guests/{id}` anonimizzava un ospite anche con soggiorni/fatture entro le finestre legali TULPS (5 anni) / fiscale (10 anni) — riprodotto dal vivo su un ospite reale con 2 fatture `PAID` del 2026. Tripla causa indipendente: path Feign errato in `BillingServiceClient` (`/last-invoice-date` invece del reale `/last-date`, 404 garantito), due metodi `public final` (`StayServiceImpl.getLastStayDateForGuest`, `InvoiceServiceImpl.getLastInvoiceDateForGuest`) che sotto proxy CGLIB girano su un'istanza mai iniettata (NPE garantita), e la logica che consuma il fallback del circuit breaker leggeva `lastStayDate()==null` come "nessun vincolo" invece che come indeterminazione bloccante — il "fail-safe" dichiarato nel Javadoc del fallback era in realtà fail-open | CRITICO | ALTA | ✅ RISOLTO |
| T-GST-07 | Elevation of Privilege | `GuestController` (delete/anonimizzazione, export Art. 20) e `GuestPrivacySettingsController` (retention policy hotel) senza alcuna `@PreAuthorize` — riprodotto dal vivo: un RECEPTIONIST poteva cancellare/anonimizzare qualunque ospite, esportare il dump PII completo, e modificare il periodo di retention GDPR dell'hotel | ALTO | ALTA | ✅ RISOLTO — **estensione 2026-08 (audit a 10 agenti)**: stesso gap trovato su `GuestController.removeIdentityDocument` (ownership già verificata, ma nessun controllo di ruolo — qualunque RECEPTIONIST poteva cancellare documenti d'identità), aggiunto `@PreAuthorize("hasAnyRole('ADMIN','OWNER')")` allineato al resto del controller |

### 4.4 frontdesk-service — reservations domain (ex reservation-service, consolidato ADR-001)

| ID | Categoria STRIDE | Threat | Impatto | Probabilità | Stato |
|----|-----------------|--------|---------|-------------|-------|
| T-RES-01 | Tampering | Double Booking (Overbooking): assenza di lock ottimistico sulle date | CRITICO | ALTA | ✅ RISOLTO |
| T-RES-01 residuo | Tampering | Trovato dall'audit a 10 agenti (2026-08): la mitigazione originale usava `PESSIMISTIC_WRITE` su una query di overlap che per una camera/data senza prenotazioni preesistenti restituisce zero righe — non blocca nulla, finestra di race ancora aperta tra due prenotazioni concorrenti sulla stessa camera/date senza conflitti preesistenti | CRITICO | ALTA | ✅ RISOLTO — nuova migrazione `V14__add_reservation_overlap_exclusion.sql`: colonne denormalizzate (`check_in_date`/`check_out_date`/`booking_blocking`) su `reservation_line_items` sincronizzate da due trigger Postgres (insert/update della riga, e re-sync su update del genitore `reservations`), vincolo `EXCLUDE USING gist` a livello DB. Tutti e 4 i path di scrittura (`createReservation`, `updateReservation`, `updateStatusAndGuests`, `createReservationFromPricedRooms`) instradati su `saveTranslatingOverlap`, che traduce la violazione del vincolo (SQLSTATE 23P01) in 409 invece di un 500 grezzo. Verificato dal vivo con SQL raw su Postgres reale (transazioni annullate) |
| T-RES-02 | Elevation of Privilege | IDOR: accesso a prenotazioni di altri hotel senza verifica ownership | CRITICO | MEDIA | ✅ RISOLTO |
| T-RES-03 | Injection | Filtri di ricerca date non validati server-side | MEDIO | BASSA | ✅ RISOLTO |

### 4.4b frontdesk-service — pricing & quotations domain

| ID | Categoria STRIDE | Threat | Impatto | Probabilità | Stato |
|----|-----------------|--------|---------|-------------|-------|
| T-PRICE-01 | Tampering | Trovato dall'audit a 10 agenti (2026-08): `RateCalendarServiceImpl`'s bulk-apply split/trim eseguiva un `UPDATE` su stagioni tariffarie preesistenti sovrapposte senza alcun lock/versione — race di lost-update tra due admin concorrenti che modificano lo stesso calendario tariffe | MEDIO | MEDIA | ✅ RISOLTO — `@Version` aggiunto a `RateSeason` (migrazione `V12`), `applySplitTrim`/`splitAroundNewRange` instradati su `saveTranslatingOverlap`/`deleteTranslatingOverlap`, che catturano sia violazioni del vincolo di esclusione (409 `RATE_SEASON_OVERLAP`) sia `ObjectOptimisticLockingFailureException` (409 `RATE_SEASON_CONCURRENT_MODIFICATION`) |
| T-QUOT-01 | Tampering | Trovato dall'audit a 10 agenti (2026-08): `QuotationServiceImpl.convertToReservation` leggeva il preventivo senza lock/versione — due chiamate `convert` concorrenti potevano entrambe superare il controllo di stato e creare due prenotazioni dallo stesso preventivo | ALTO | MEDIA | ✅ RISOLTO — `@Version` aggiunto a `Quotation` (migrazione `V13`), `saveAndFlush` finale avvolto in try/catch che traduce `ObjectOptimisticLockingFailureException` in 409 `QUOTATION_ALREADY_ACCEPTED` |

### 4.5 frontdesk-service — stays domain (ex stay-service, consolidato ADR-001)

| ID | Categoria STRIDE | Threat | Impatto | Probabilità | Stato |
|----|-----------------|--------|---------|-------------|-------|
| T-STAY-01 | Elevation of Privilege | Check-in senza verifica stato prenotazione (es. CANCELLED) | ALTO | MEDIA | ✅ RISOLTO |
| T-STAY-02 | Repudiation | Assenza di audit trail per operazioni check-in/check-out | ALTO | ALTA | ✅ RISOLTO |
| T-STAY-03 | Information Disclosure | Report Alloggiati: trasmissione dati PS senza verifica firma/TLS | CRITICO | BASSA | ✅ RISOLTO |
| T-STAY-04 | Elevation of Privilege | getAllStays/getStayById/checkOut/getStaysByReservationId senza scoping hotelId: un RECEPTIONIST poteva leggere o modificare (check-out) stay di qualsiasi hotel via lista paginata, UUID indovinato/enumerato, o reservationId cross-hotel | CRITICO | ALTA | ✅ RISOLTO |
| T-STAY-05 | Information Disclosure / Elevation of Privilege | Report Alloggiati Web (findByActualCheckInTimeBetween) senza scoping hotelId: il download txt non aveva nemmeno @PreAuthorize (qualunque utente autenticato di qualunque hotel scaricava PII — nome, data nascita, documento — di tutti gli ospiti di tutti gli hotel per una data); JSON export e invio "Invia a Questura" richiedevano ADMIN/OWNER ma restituivano/sottomettevano comunque dati cross-tenant, incluso un invio legale alla Polizia di Stato con ospiti di altri hotel mescolati sotto le credenziali PS dell'hotel chiamante | CRITICO | ALTA | ✅ RISOLTO |
| T-STAY-06 | Information Disclosure | Pre-fill check-in (`GET /stays/guest/{guestId}/latest`) interrogava l'ultimo soggiorno CHECKED_OUT di un ospite senza scoping hotelId — un receptionist di un hotel poteva vedere/riusare per il pre-fill la storia soggiorni di un ospite presso un **altro hotel** della piattaforma | ALTO | MEDIA | ✅ RISOLTO |
| T-STAY-07 | Tampering | `StayRequest.status` era un campo client-controllato che arrivava senza filtro all'entità `Stay` persistita — `checkIn()` forzava `CHECKED_IN` solo se lo status sottomesso era `null`/`EXPECTED`. Un client che inviava `POST /api/v1/stays` con `status: CHECKED_OUT` otteneva una stay già "nata" checked-out, saltando completamente il guard `BILLING_NOT_PAID` di `checkOut()` — nessun addebito richiesto. Peggio: la camera veniva comunque impostata `OCCUPIED` dal passo incondizionato della saga di check-in, ma non transizionava mai a `DIRTY` (accade solo dentro `checkOut()`, mai invocato) — `RoomServiceImpl` rifiuta esplicitamente di liberare `OCCUPIED` per altra via, quindi la camera restava bloccata permanentemente finché non interveniva un DBA. Trovato dall'audit di sicurezza a 10 sotto-agenti paralleli (`security-audit/`, 2026-08, Finding #2 CRITICAL) | CRITICO | ALTA | ✅ RISOLTO |

### 4.6 billing-service

| ID | Categoria STRIDE | Threat | Impatto | Probabilità | Stato |
|----|-----------------|--------|---------|-------------|-------|
| T-BILL-01 | Elevation of Privilege | IDOR: accesso a fatture di altri ospiti/hotel | CRITICO | MEDIA | ✅ RISOLTO |
| T-BILL-02 | Tampering | Importi non validati server-side (negative amounts, overflow) | ALTO | BASSA | ✅ RISOLTO |
| T-BILL-03 | Repudiation | Assenza di log immutabile delle transazioni di pagamento | ALTO | ALTA | ✅ RISOLTO |
| T-BILL-04 | Information Disclosure | Report finanziario Owner (`GET /api/v1/reports/owner`) interrogava le fatture senza scoping hotelId — qualunque OWNER/ADMIN di un hotel qualsiasi leggeva fatturato, conteggio fatture e lista fatture (dati ospite inclusi) di tutti gli altri hotel della piattaforma | CRITICO | ALTA | ✅ RISOLTO |
| T-BILL-05 | Denial of Service | `ThymeleafPdfTemplateRenderer` (pdf-template-engine, generazione PDF fattura) rileggeva e ri-subsettava i font Noto Sans dal classpath ad ogni singola chiamata `render()` — unico punto di allocazione Metaspace per-richiesta nell'intera pipeline, nessun limite sul numero di generazioni concorrenti/sequenziali. Un volume anche moderato di richieste (traffico legittimo o un singolo chiamante che ripete la generazione) esauriva il Metaspace e crashava la JVM del servizio (CWE-400, uncontrolled resource consumption) — mascherato in produzione da un secondo bug indipendente (GuestClient senza discovery configurata, "Unknown Guest" su ogni fattura) che rendeva la funzionalità raramente esercitata fino alla sua riparazione | ALTO | MEDIA | ✅ RISOLTO |
| T-BILL-06 | Tampering / Repudiation | `Invoice` non aveva alcun campo immutabile oltre `createdAt`: `totalAmount`, `invoiceNumber`, `documentType`, le `charges` restavano modificabili via API anche dopo che la fattura era già stata esportata/consegnata come XML FatturaPA — per la normativa italiana una fattura emessa si corregge con nota di credito, non si edita. Inoltre non esisteva alcuno storico di cosa fosse stato effettivamente esportato (`sdi_status` era un solo campo senza log): una rigenerazione successiva dell'XML poteva non coincidere più con ciò che fu davvero consegnato al commercialista se i dati sottostanti cambiavano nel frattempo, senza modo di dimostrare in un controllo fiscale quale fosse il documento originale | ALTO | MEDIA | ✅ RISOLTO — nuova tabella `invoice_fiscal_exports` (snapshot immutabile: bytes XML + hash SHA-256 + timestamp per ogni export); guard applicativo in `InvoiceServiceImpl` blocca `addCharge`/`updateDocumentType` con `409 INVOICE_LOCKED_AFTER_EXPORT` non appena esiste almeno un export registrato per la fattura. Nota di credito per correggere fatture già esportate resta esplicitamente fuori scope (da pianificare separatamente) |
| T-BILL-07 | Elevation of Privilege | `InvoiceController` senza alcuna `@PreAuthorize` sulle operazioni fiscalmente sensibili — riprodotto dal vivo: un RECEPTIONIST poteva cambiare `documentType` FATTURA↔RICEVUTA, generare e bloccare permanentemente un export FatturaPA, impostare lo stato di trasmissione SDI, e lanciare l'export batch per il commercialista, senza alcun controllo di autorizzazione oltre l'autenticazione | ALTO | ALTA | ✅ RISOLTO |

### 4.7 fb-service

| ID | Categoria STRIDE | Threat | Impatto | Probabilità | Stato |
|----|-----------------|--------|---------|-------------|-------|
| T-FB-01 | Elevation of Privilege | IDOR: ordini F&B non legati a hotel_id, accessibili cross-hotel | ALTO | MEDIA | ✅ RISOLTO |
| T-FB-02 | Tampering | Prezzi degli ordini non ricalcolati server-side (client-side price tampering) | CRITICO | ALTA | ✅ RISOLTO |
| T-FB-03 | Elevation of Privilege | `SecurityConfig` mancava `@EnableMethodSecurity` — i 3 `@PreAuthorize("hasAnyRole('ADMIN','OWNER')")` su `MenuItemController` (create/update/delete) non venivano mai valutati da Spring: qualunque ruolo autenticato (incluso RECEPTIONIST) poteva creare, modificare o cancellare voci di menu | ALTO | ALTA | ✅ RISOLTO |

### 4.8 config-service

| ID | Categoria STRIDE | Threat | Impatto | Probabilità | Stato |
|----|-----------------|--------|---------|-------------|-------|
| T-CFG-01 | Information Disclosure | Endpoint `/actuator` esposti senza autenticazione | CRITICO | ALTA | ✅ RISOLTO |
| T-CFG-02 | Information Disclosure | Segreti in chiaro nei file di configurazione (JWT secret, HMAC key, DB password) | CRITICO | ALTA | ✅ RISOLTO |
| T-CFG-03 | Spoofing | Nessuna autenticazione tra config-service e microservizi consumer | ALTO | MEDIA | ✅ RISOLTO |

### 4.9 Frontend (React)

| ID | Categoria STRIDE | Threat | Impatto | Probabilità | Stato |
|----|-----------------|--------|---------|-------------|-------|
| T-FE-01 | Tampering | XSS: output di dati utente non encoded in rendering dinamico | ALTO | MEDIA | ✅ RISOLTO |
| T-FE-02 | Information Disclosure | Token JWT eventualmente esposto in localStorage o state non protetto | CRITICO | BASSA | ✅ RISOLTO |
| T-FE-03 | Elevation of Privilege | Broken Access Control frontend: route admin accessibili nascondendo solo elementi UI | MEDIO | ALTA | ✅ RISOLTO |
| T-FE-04 | Tampering | Assenza Content-Security-Policy: possibilità di inject di script esterni | ALTO | MEDIA | ✅ RISOLTO — **rivalutazione 2026-08 (audit a 10 agenti)**: `style-src 'unsafe-inline'` confermato necessario, non un fix mancato — l'app stessa muta stili inline via JS a runtime (`settingsStore.ts` scala il font MD3 con `document.documentElement.style.setProperty`, più componenti nascondono immagini/iframe rotti con `el.style.display='none'`), e la CSP `style-src` governa sia gli attributi `style=""` sia le mutazioni CSSOM script-driven. Commento nel file aggiornato per riflettere la ragione reale invece del solo "Tailwind" |
| T-FE-05 | Information Disclosure | Trovato dall'audit a 10 agenti (2026-08): assenti gli header di cross-origin isolation (COOP/COEP/CORP) e `Server: nginx/x.x.x` esposto in ogni risposta (version disclosure) | MEDIO | MEDIA | ✅ RISOLTO — `Cross-Origin-Opener-Policy: same-origin`, `Cross-Origin-Embedder-Policy: require-corp`, `Cross-Origin-Resource-Policy: same-origin` aggiunti a tutti e 4 i blocchi header di `nginx.conf` (rispettando il bug di ereditarietà `add_header` già noto, GAP-10); `server_tokens off;` aggiunto al blocco server. Verificato: nessuna risorsa cross-origin nel frontend (font bundlato via npm, non Google Fonts; unico iframe usage — preview PDF fattura/preventivo — è same-origin via `blob:` o `/api/`) che COEP `require-corp` potrebbe rompere |

### 4.10 frontdesk-service — rooms domain (ex inventory-service, consolidato ADR-001)

| ID | Categoria STRIDE | Threat | Impatto | Probabilità | Stato |
|----|-----------------|--------|---------|-------------|-------|
| T-ROOM-01 | Tampering / Elevation of Privilege | createRoom/updateRoom non derivavano hotelId dal contesto autenticato (si fidavano del body, frontend inviava un placeholder hardcoded tutto-zero); getAllRooms non filtrava per hotelId, ritornando camere di tutti gli hotel | CRITICO | MEDIA | ✅ RISOLTO |
| T-ROOM-02 | Elevation of Privilege / Tampering | room_types (tabella dal V1 baseline, pre-data la convenzione hotel_id) non aveva alcuna colonna hotel_id: qualunque ADMIN/OWNER di qualunque hotel poteva leggere, modificare (prezzo, nome, capienza) o cancellare (soft-delete) il catalogo tipologie-camera di un hotel altrui via UUID noto/enumerato — IDOR + scrittura cross-tenant, non solo un filtro mancante. UNIQUE(name) globale impediva anche a due hotel indipendenti di nominare entrambi una tipologia "Deluxe" | CRITICO | MEDIA | ✅ RISOLTO |

### 4.11 notification-service

| ID | Categoria STRIDE | Threat | Impatto | Probabilità | Stato |
|----|-----------------|--------|---------|-------------|-------|
| T-NOTIF-01 | Spoofing | Servizio non instradato dal gateway (raggiungibile solo dalla rete interna Docker), 3 endpoint solo-scrittura (`POST /internal/notifications/{reservation-confirmed,checkin,checkout}`) che innescano invio email transazionali con PII ospite (nome, indirizzo email) nel corpo/subject. Mai censito in questo documento nonostante fosse protetto fin dall'introduzione di T-GW-08 dallo stesso `InternalAuthFilter` degli altri 5 servizi — un chiamante privo di firma HMAC valida potrebbe altrimenti innescare invii email arbitrari (subject/corpo/destinatario controllati dal chiamante) usando l'infrastruttura email legittima dell'hotel, un vettore di phishing/spoofing verso gli ospiti | ALTO | BASSA | ✅ RISOLTO |

### 4.12 Infrastruttura — Backup & Disaster Recovery

| ID | Categoria STRIDE | Threat | Impatto | Probabilità | Stato |
|----|-----------------|--------|---------|-------------|-------|
| T-OPS-01 | Denial of Service (dei dati) | RPO fisso a 24h (dump `pg_dumpall` giornaliero pianificato): un disastro del volume Postgres poco prima del prossimo dump pianificato perde fino a un'intera giornata di lavoro (prenotazioni, check-in, fatture). Nessuna verifica automatica che un backup sia davvero ripristinabile — solo un drill manuale una tantum (31/07). Mai censito in questo documento nonostante l'asset esistesse da GAP-4 in poi | ALTO | MEDIA | ✅ RISOLTO |
| T-OPS-02 | Information Disclosure | Cambio del modello di cifratura backup da asimmetrico (`age`, solo chiave pubblica sull'host prod) a simmetrico (pgBackRest `repo-cipher-pass`) — necessario per WAL archiving continuo, ma significa che compromettere l'host di produzione oggi espone anche la chiave per decifrare i backup passati e futuri, proprietà che lo schema precedente escludeva esplicitamente per design | MEDIO | BASSA | ⚠️ RISCHIO ACCETTATO |

---

## 5. Risk Matrix

```
IMPATTO
  │
  │  CRITICO  │ T-AUTH-02 │ T-AUTH-07 │ T-GW-01  │ T-GST-01  │ T-RES-01  │ T-CFG-01  │ T-BILL-04 │ T-GST-06  │ T-AUTH-11 │ T-RES-01-residuo │ T-STAY-07 │ T-GW-11 │
  │  ALTO     │ T-AUTH-01 │ T-AUTH-08 │ T-GW-03  │ T-GST-02  │ T-STAY-01 │ T-BILL-01 │ T-GST-07  │ T-BILL-07 │ T-AUTH-12 │ T-GW-10 │ T-QUOT-01 │
  │  MEDIO    │ T-AUTH-05 │ T-AUTH-06 │ T-AUTH-09 │ T-GW-05  │ T-GST-04  │ T-AUTH-13 │ T-PRICE-01 │ T-FE-05 │           │
  │  ALTO     │           │          │ T-GST-05  │           │           │
  │  BASSO    │ T-AUTH-10 │          │           │           │           │
  │           ├───────────┴──────────┴───────────┴───────────┴───────────
  │                BASSA        MEDIA          ALTA
  │                               PROBABILITÀ
```

**Priorità interventi**: CRITICO+ALTA > CRITICO+MEDIA > ALTO+ALTA > ...

---

## 6. Mitigations — Tabella Tracciamento

Questa tabella viene aggiornata ad ogni commit di hardening sul branch `feature/secure-coding-hardening`.

| Threat ID | Intervento | Servizio/File | Commit | OWASP | Stato |
|-----------|------------|---------------|--------|-------|-------|
| T-AUTH-01 | Messaggi errore login uniformi (no user enumeration) | auth-service/AuthServiceImpl.java | baseline (main) | A07 | ✅ |
| T-AUTH-02 | Account lockout dopo 5 tentativi falliti, 15 min | auth-service/AuthServiceImpl.java | a90e462 | A07 | ✅ |
| T-AUTH-03 | BCryptPasswordEncoder strength 12 (vs default 10); lazy rehash on login via passwordEncoder.upgradeEncoding(): se l'hash stored ha cost < 12, viene ricalcolato con la password in chiaro al momento del login riuscito e salvato in DB | auth-service/SecurityConfig.java, AuthServiceImpl.java | 4e1f1e9 | A02 | ✅ |
| T-AUTH-04 | Refresh token rotation con blacklist Redis: access token TTL 15 min; refresh token TTL 7 giorni con jti UUID; POST /refresh valida jti, lo blacklista in Redis (rt:blacklist:<jti> con TTL residuo), emette nuovo token pair (rotation); logout blacklista il refresh token; frontend Axios interceptor ritenta la richiesta originale dopo refresh silenzioso | auth-service/JwtService.java, RefreshTokenService.java, RefreshTokenServiceImpl.java, AuthServiceImpl.java, AuthController.java, config/auth-service.yml, frontend/api.ts | 5ce3010 | A07 | ✅ |
| T-AUTH-04 residuo | Cambio password non revocava le sessioni attive: i refresh token emessi prima del cambio password restavano validi per 7 giorni. Fix: (1) Flyway V4 aggiunge token_version INT DEFAULT 0 a user_account; (2) claim tv embedded in ogni JWT via JwtService; (3) POST /api/v1/auth/change-password verifica current password (second factor), incrementa tokenVersion in DB, sovrascrive Redis key user:tv:<username> con nuovo valore; (4) AuthServiceImpl.refresh() confronta tv del JWT con Redis: se storedTv>=0 e jwtTv!=storedTv → 401. Login/register rinfrescano la Redis key. Migrazione graceful: storedTv=-1 (key assente) salta il check. 6 nuovi test. | auth-service/V4__add_token_version.sql, UserAccount.java, JwtService.java, RefreshTokenService.java, RefreshTokenServiceImpl.java, AuthServiceImpl.java, AuthController.java, ChangePasswordRequest.java | b473265 | A07 | ✅ |
| T-AUTH-05 | Structured SLF4J audit log (@Slf4j) in AuthServiceImpl e AuthController: REGISTER_SUCCESS, LOGIN_SUCCESS, LOGIN_FAILED (USER_NOT_FOUND / BAD_PASSWORD + counter), ACCOUNT_LOCKED (con expiry), LOGIN_BLOCKED, LOGOUT (username da JWT cookie) | auth-service/AuthServiceImpl.java, AuthController.java | ad441e9 | A09 | ✅ |
| T-AUTH-06 | Trovato durante un giro di navigazione E2E reale (`docs/LIVE_E2E_AUDIT_2026-07.md`, BUG-5): un utente creato con password provvisoria (`mustChangePassword=true`, admin-created o dopo reset password) veniva reindirizzato a `/settings/password` solo dal frontend — navigando direttamente a un'altra URL o chiamando l'API direttamente, le credenziali provvisorie restavano pienamente valide su ogni endpoint, vanificando la rotazione forzata. Il claim non esisteva nel JWT (solo colonna DB, letta esclusivamente da `/auth/me`), nessun filtro lato server lo controllava mai. Fix: `mustChangePassword` aggiunto come claim JWT firmato in `JwtService.generateToken/generateRefreshToken`, popolato a register/login/refresh/change-password (a `changePassword` viene sempre emesso `false`, coerente col reset appena avvenuto). `AuthenticationFilter` (gateway, unico punto che copre le route business billing/guest/frontdesk/fb + `/api/v1/auth/users/**`) rifiuta con 403 `PASSWORD_CHANGE_REQUIRED` qualunque richiesta con claim `true` il cui path non è nell'allow-list (`/api/v1/auth/change-password`, `/me`, `/logout`, `/refresh`) — check eseguito prima della RBAC così il ruolo non può bypassare l'obbligo. Le route pubbliche di auth-service (login/refresh/change-password/me/logout) non passano comunque da `AuthenticationFilter` (nessun filtro su quella route in `api-gateway.yml`, `permitAll` per design), quindi l'allow-list è un backstop difensivo, non il gate primario per quei path. Nuovi test: `AuthServiceImplTest` (claim propagato a login con `mustChangePassword=true`, azzerato dopo `changePassword`), `AuthenticationFilterTest` nested `MustChangePasswordTests` (blocco business endpoint anche per ADMIN, claim assente/false passa, path allow-listed passa nonostante il claim) | auth-service/service/JwtService.java, AuthServiceImpl.java, api-gateway/filter/AuthenticationFilter.java | a82ce14 | A01, A07 | ✅ |
| T-AUTH-07 | Trovato durante un audit E2E reale (`docs/LIVE_E2E_AUDIT_2026-07.md`, BUG-12): il bottone "Activate" su un utente disattivato falliva sempre con 404. `UserManagementServiceImpl.activateUser` cercava il target con `userRepository.findById(targetUserId)`, ma `UserAccount` porta `@SQLRestriction("active = true")` a livello di classe — un filtro Hibernate iniettato in ogni query generata per l'entità, comprese le query HQL-derivate ereditate come `findById`, quindi un utente `active=false` non veniva mai trovato. Il commento nel codice originale ("Must query including inactive — use raw query bypassing @SQLRestriction") descriveva l'intento corretto ma il codice sottostante non lo implementava. Test esistente (`activateUserShouldSetActiveTrue`) passava comunque perché mockava `findById` direttamente con Mockito, bypassando del tutto Hibernate — non avrebbe mai potuto catturare il bug reale. Fix: nuova query nativa `findByIdAndHotelIdIncludingInactive` in `UserAccountRepository` — le query native non sono soggette a `@SQLRestriction` (a differenza di HQL/JPQL), primo caso nel repo, mantenuto hotel-scoped per non introdurre un leak cross-tenant; nome conforme alla regola ArchUnit `TenantIsolationArchTest` (deve contenere `HotelId`). Test riscritti per mockare il nuovo metodo | auth-service/repository/UserAccountRepository.java, service/UserManagementServiceImpl.java | [pending] | A04 | ✅ |
| T-AUTH-08 | Trovato durante un audit E2E reale (`docs/LIVE_E2E_AUDIT_2026-07.md`, BUG-8). L'interceptor Axios responsabile del refresh silenzioso (T-AUTH-04) escludeva esplicitamente `/api/v1/auth/me` dal retry-dopo-refresh su 401, con la motivazione "prevent loops" — valida per `/login` (non ha senso un refresh su un login fallito) ma sovra-estesa a `/me`, endpoint di sola lettura chiamato una sola volta al mount dell'app (`App.tsx`). Effetto: l'access token dura 15 minuti, il refresh token 7 giorni; ma se l'utente ricarica la pagina (F5, link diretto, nuova scheda) dopo la scadenza dei 15 minuti, `GET /me` fallisce 401, l'interceptor non tenta il refresh, l'utente viene reindirizzato a `/login` nonostante il refresh token sia ancora pienamente valido per altri giorni. Fix: rimossa l'esclusione di `/me` dalla condizione — un 401-poi-refresh-poi-retry su un endpoint read-only non può creare un loop (il refresh stesso, se fallisce, chiama `performLogout()` una sola volta tramite il branch `/refresh` dedicato, mai un secondo refresh) | frontend/src/services/api.ts:74-85 | [pending] | A07 | ✅ |
| T-AUTH-09 | Trovato durante un audit E2E reale (`docs/LIVE_E2E_AUDIT_2026-07.md`, BUG-13). `AuthServiceImpl.changePassword` è specificamente progettato per non disconnettere l'utente (T-AUTH-04 residuo): emette un nuovo token pair valido subito dopo l'incremento di `tokenVersion`, proprio per garantire continuità di sessione dopo un cambio password volontario. Ma `SettingsPassword.tsx` ignorava questo intento: dopo una risposta 200 OK (nuovi cookie già impostati dal backend), chiamava comunque `logout()`+redirect a `/login` incondizionatamente, buttando via la sessione appena rinnovata — l'utente doveva ridigitare da capo la password appena impostata. Non un rischio di sicurezza (il comportamento attuale era "più sicuro" per puro accidente), ma contraddiceva esplicitamente un comportamento backend deliberatamente progettato e già documentato. Fix: rimossa la chiamata a `logout()`; dopo il successo, la sessione viene ri-sincronizzata (`fetchMe`+`checkAuth`, così un eventuale `mustChangePassword` stale nello store viene azzerato) e l'utente resta autenticato, con navigazione alla dashboard (se veniva dal flusso forzato) o alla pagina precedente altrimenti | frontend/src/pages/Settings/SettingsPassword.tsx:57-76 | [pending] | A07 | ✅ |
| T-AUTH-10 | Trovato analizzando un finding OSINT (Actuator che tornava 500 invece di 404): la causa non era Actuator ma un `@ExceptionHandler(Exception.class)` catch-all senza handler più specifici per alcune eccezioni Spring native, replicato/duplicato in 5 dei 6 servizi. Sistemato completando la migrazione (rimasta a metà dall'1 agosto) di auth-service/frontdesk-service/notification-service a estendere `common-web-lib/AbstractProblemDetailAdvice` invece di duplicare gli handler localmente — elimina il drift che aveva causato il bug. Effetto diretto su auth-service: `AccessDeniedException` da `@PreAuthorize` su `UserManagementController` ora eredita l'handler condiviso e torna 403 invece di 500 (l'accesso era comunque sempre stato negato correttamente, solo lo status HTTP era sbagliato). Aggiunti anche 4 handler mancanti alla base condivisa (`NoResourceFoundException`→404, `MethodArgumentTypeMismatchException`→400, `HttpRequestMethodNotSupportedException`→405, `HttpMediaTypeNotSupportedException`→415) e, in frontdesk-service, `FeignException`→502 (stesso bug gemello, mai gestito localmente). frontdesk-service mantiene il proprio `traceId` di correlazione log↔risposta come override esplicito del catch-all ereditato | common-web-lib/exception/AbstractProblemDetailAdvice.java, auth/frontdesk/notification-service/exception/GlobalExceptionHandler.java | 8c0fa2c | A01 | ✅ |
| T-GW-01 | Verifica HMAC-SHA256 su X-Internal-Signature (InternalAuthFilter) | api-gateway + tutti i servizi | baseline (main) | A01 | ✅ |
| T-GW-07 | Messaggio HMAC esteso da "username:role" a "username:role:hotelId": AuthenticationFilter.computeHmac aggiornato; tutti e 6 gli InternalAuthFilter aggiornati in isSignatureValid e computeHmac; nuovo test shouldProduceDifferentSignatureForDifferentHotelId verifica che la firma cambia al variare del tenant | api-gateway/AuthenticationFilter.java, guest/billing/reservation/fb/stay/inventory-service/InternalAuthFilter.java, AuthenticationFilterTest.java | 5bdc594 | A01 | ✅ |
| T-GW-08 | Messaggio HMAC esteso da "username:role:hotelId" a "username:role:hotelId:timestamp:nonce": AuthenticationFilter genera timestamp (epoch millis) + nonce (UUID) random ad ogni richiesta, iniettati come X-Auth-Timestamp/X-Auth-Nonce. Tutti e 5 gli InternalAuthFilter (auth/billing/fb/frontdesk/guest) validano: (1) finestra di tolleranza 60s sul timestamp, (2) nonce non già reclamato tramite nuovo NonceStore backed by Redis (SETNX via StringRedisTemplate.opsForValue().setIfAbsent, TTL 120s). Redis esteso a backend-network (oltre a gateway-network) così tutti i validator lo raggiungono; auth-service riusa la dipendenza/config Redis già presente per i refresh token, gli altri 4 servizi l'hanno aggiunta su namespace separato (internal-auth:nonce:*). SpotBugs EI_EXPOSE_REP2 su RedisNonceStore sopresso (bean Spring singleton, stesso pattern già usato per AlloggiatiWebSenderServiceImpl). Nuovi test: 4 casi anti-replay in InternalAuthFilterTest (fb-service) — timestamp stale, timestamp futuro, replay esatto rifiutato, due nonce diversi sullo stesso payload entrambi accettati; AuthenticationFilterTest verifica generazione header e unicità nonce; StayControllerSecurityTest aggiornato al nuovo payload a 5 campi. **Fix 2026-06-18 (review post-merge)**: il rollout iniziale (commit 5dd8ed8) aggiornava solo l'AuthenticationFilter del gateway e i 5 InternalAuthFilter, ma non FeignHeaderConfig — l'interceptor che ogni servizio usa per firmare le proprie chiamate Feign in uscita verso altri servizi (frontdesk→billing/guest, fb→billing/frontdesk, guest→billing/frontdesk, billing→guest/frontdesk). Senza timestamp/nonce nel payload firmato, ogni chiamata interservizio sarebbe stata rifiutata con 401 dal validator ricevente — regressione funzionale, non solo di sicurezza. Trovato da una review di sicurezza mirata post-merge (subagent indipendente) prima del merge su main. Corretto in tutti e 4 i FeignHeaderConfig (frontdesk/fb/guest/billing); scoperto anche un bug pre-esistente e separato in billing-service, che non inviava mai X-Internal-Signature. Aggiunti FeignHeaderConfigTest per tutti e 4 i servizi (nessuno esisteva prima) | api-gateway/AuthenticationFilter.java, auth/billing/fb/frontdesk/guest-service/security/InternalAuthFilter.java + NonceStore.java + RedisNonceStore.java + SecurityConfig.java, billing/fb/frontdesk/guest-service/config/FeignHeaderConfig.java, docker-compose.yml, config-service/config/{billing,fb,frontdesk,guest}-service.yml | 5dd8ed8, b80a303 | A01 | ✅ |
| T-GW-09 | Aggiunta "/api/v1/rate-calendar" a WRITE_RESTRICTED_PREFIXES: le scritture (bulk-apply prezzi) restano riservate ad ADMIN/OWNER anche se il @PreAuthorize a livello controller venisse bypassato o rimosso per errore, stesso pattern già in uso per /api/v1/room-types; la lettura (GET) resta aperta a tutti i ruoli operativi. Deliberatamente rimandato fuori dal commit originario della feature rate-calendar (eeb0182, su main) perché tocca AuthenticationFilter.java, file di enforcement RBAC che la regola di branching di CLAUDE.md richiede sul branch di hardening. 3 nuovi test in AuthenticationFilterTest (RECEPTIONIST bloccato dal bulk-apply, RECEPTIONIST può leggere, ADMIN può bulk-apply) | api-gateway/AuthenticationFilter.java | 2bf58be | A01 | ✅ |
| T-FE-02 | Token JWT in httpOnly cookie (Secure + SameSite=Strict) | auth-service/AuthController.java | baseline (main) | A02 | ✅ |
| T-GW-03 | SecurityHeadersFilter GlobalFilter: HSTS, X-Content-Type-Options, X-Frame-Options, Referrer-Policy, Permissions-Policy, CSP default-src 'none' su tutte le risposte del gateway | api-gateway/SecurityHeadersFilter.java | b901a9f | A05 | ✅ |
| T-GW-04 | allowedHeaders wildcard rimosso: whitelist esplicita [Content-Type, X-CSRF-Token]; maxAge: 3600; allowedOrigins già vincolato a ${GW_CORS_ALLOWED_ORIGINS:http://localhost:5173} — mai wildcard; in produzione nginx proxies /api/ same-origin, CORS non necessario. **Estensione 2026-08 (audit a 10 agenti)**: `docker-compose.yml` aveva `GW_CORS_ALLOWED_ORIGINS` come literal hardcoded invece di `${VAR}`-sostituibile come ogni altra variabile del file — reso `${GW_CORS_ALLOWED_ORIGINS:-http://localhost:5173}` | config-service/config/api-gateway.yml, docker-compose.yml | 73e2788, 01f76a9 | A05 | ✅ |
| T-GW-05 | Double Submit Cookie: CsrfFilter GlobalFilter nell'api-gateway valida che X-CSRF-Token header == csrf_token cookie su ogni richiesta mutante (POST/PUT/PATCH/DELETE); esclusi /auth/login e /auth/register (pre-auth); auth-service emette csrf_token cookie non-httpOnly (Secure, SameSite=Strict) su login/register/refresh e la cancella al logout; frontend Axios interceptor legge il cookie e inietta l'header; 10 test in CsrfFilterTest. **Fix 2026-06-19 (QA browser, riproducibile)**: il cookie csrf_token aveva lo stesso maxAge dell'access token (15 min) ma solo i 401 triggerano il refresh silenzioso lato client — un csrf_token scaduto bloccava ogni richiesta mutante con 403 MISSING_TOKEN, **incluso POST /auth/refresh stesso** (non escluso come login/register), lasciando l'utente senza alcun percorso di recovery (deadlock: il solo endpoint che rinnova il cookie è bloccato dalla sua stessa scadenza). Non è una vulnerabilità exploitable (nessun bypass, nessuna fuga di dati) — è un falso positivo che blocca sessioni legittime su flussi lunghi (es. form check-in Alloggiati). Analizzato con LLM Council (3 opzioni: esentare /refresh dal CSRF, allungare la vita del cookie, far rilevare il 403 al frontend); scelta A+B: (A) /api/v1/auth/refresh aggiunto a EXCLUDED_PATHS — già protetto da refresh_token httpOnly, path-scoped, SameSite=Strict, ridondante richiedere anche il CSRF; (B) maxAge del cookie csrf_token portato da ACCESS_COOKIE_MAX_AGE (900s) a REFRESH_COOKIE_MAX_AGE (604800s) — la sicurezza del pattern double-submit-cookie dipende dall'imprevedibilità del token, non dalla sua freschezza, quindi non c'è motivo di farlo scadere al ritmo dell'access token. Opzione (C, detection lato frontend) scartata come prioritaria: più codice, più superficie per bug nel recovery stesso, beneficio marginale una volta applicate A+B. Nuovo test refreshExcluded in CsrfFilterTest | api-gateway/CsrfFilter.java, auth-service/AuthController.java, frontend/api.ts | e146b17, [pending] | A01 | ✅ |
| T-GW-06 | Flyway V3 aggiunge hotel_id UUID NOT NULL a user_account; UserAccount entity + RegisterRequest + JwtService.generateToken/Refresh(username,role,hotelId) + CLAIM_HOTEL_ID constant; AuthenticationFilter estrae hotelId dal JWT e inietta X-Auth-Hotel (401 se claim assente); stay-service e inventory-service InternalAuthFilter allineati con HEADER_HOTEL + null-check + auth.setDetails(hotelId); AuthServiceImplTest + AuthenticationFilterTest aggiornati | auth-service/V3, UserAccount.java, JwtService.java, AuthServiceImpl.java; api-gateway/AuthenticationFilter.java; stay-service/InternalAuthFilter.java; inventory-service/InternalAuthFilter.java | b469e21 | A01 | ✅ |
| T-GST-01 | IDOR fix: resolveGuest(id, hotelId) → findByIdAndHotelId, 404 anche per guest di altri hotel (no enumeration) | guest-service/GuestServiceImpl.java, GuestRepository.java | b483eac | A01 | ✅ |
| T-GST-03 | hotel_id su tabella guests (Flyway V2) + tutte le query repository scoped per hotelId; X-Auth-Hotel letto da InternalAuthFilter e propagato via FeignHeaderConfig | guest-service/V2__add_hotel_id_to_guests.sql, GuestRepository.java, InternalAuthFilter.java, FeignHeaderConfig.java | b483eac | A01 | ✅ |
| T-GST-02 | @Pattern su firstName/lastName (NAME_PATTERN), phone (PHONE_PATTERN), address (TEXT_SAFE_PATTERN), city/country (LOCATION_PATTERN); @Past su dateOfBirth; @Pattern(DOCUMENT_NUMBER_PATTERN) + @Past/@FutureOrPresent su IdentityDocumentRequestDTO | guest-service/GuestRequest.java, IdentityDocumentRequestDTO.java, ValidationConstants.java | fb6fdff | A03 | ✅ |
| T-GST-04 | GuestMapper.toEntity() con @Mapping(ignore=true) espliciti su id, hotelId, identityDocuments, active, createdAt, updatedAt — protezione mass-assignment dichiarativa | guest-service/GuestMapper.java | fb6fdff | A04 | ✅ |
| T-GST-05 | Flyway V4 (gdpr_consent_date su guests, backfill da created_at) + V5 (guest_privacy_settings, min 5 anni TULPS validato con @Min). deleteGuest() sostituito con hard-anonymize + guardia legale duale: TULPS (frontdesk-service GET /stays/guest/{id}/last-date, @NonNull con circuit-breaker fail-closed) e fiscale (billing-service GET /invoices/guest/{id}/last-invoice-date); HTTP 451 + GdprLegalHoldException con unlocksAt e legalBasis. Guardia aggiuntiva su prenotazioni attive: frontdesk-service GET /reservations/guest/{id}/active. GuestPrivacySettingsController (GET/PUT /api/v1/guests/settings). GuestRetentionJobServiceImpl (@Scheduled 02:00, pre-filtro conservativo FISCAL_MIN=10y, fail-closed). FeignHeaderConfig fix HMAC (era mancante). 17 test boundary-value. **Fix 2026-06-18 (ADR-001 frontdesk consolidation):** trovati 2 bug di wiring mai funzionanti — path StayServiceClient errato (`last-stay-date` invece di `last-date`, sempre 404) ed endpoint `/reservations/guest/{id}/active` mai implementato (sempre 404) — la guardia falliva sempre "closed" (bloccava sempre la cancellazione) indipendentemente dallo stato reale. Corretti: path client allineato al controller reale; nuovo endpoint implementato (ReservationRepository.existsByGuestIdAndHotelIdAndStatusNotIn + ReservationServiceImpl.hasActiveReservations + ReservationController). | guest-service, frontdesk-service (ex stay-service + reservation-service), billing-service | 56109eb, 066f611 | A04 | ✅ |
| T-GST-06 | Trovato dal secondo giro di test esplorativo dal vivo (`docs/EXPLORATORY_TEST_2026-08.md`, Round 2 bug #1): la stessa guardia legale di T-GST-05 si era di nuovo rotta, questa volta con una tripla causa indipendente accumulata su ulteriori refactoring. Fix: (1) `BillingServiceClient.getLastInvoiceDate` path corretto da `/api/v1/invoices/guest/{guestId}/last-invoice-date` a `/api/v1/invoices/guest/{guestId}/last-date` (combacia col controller reale); (2) rimosso `final` da `InvoiceServiceImpl.getLastInvoiceDateForGuest` e `StayServiceImpl.getLastStayDateForGuest` — sotto proxy CGLIB (`proxy-target-class=true`, introdotto da `@EnableMethodSecurity`) un metodo `final` non è overridabile dalla sottoclasse proxy e gira sull'istanza-guscio mai iniettata via DI, NPE garantita; (3) `GuestServiceImpl.verifyLegalHolds` e `GuestRetentionJobServiceImpl.shouldAnonymise` ora trattano esplicitamente `hasStays()/hasInvoices()=true` con data `null` (fallback del circuit breaker) come vincolo bloccante per indeterminazione, mai come "nessun vincolo" — prima il caso indeterminato cadeva silenziosamente nel ramo "nessun blocco". Verificato dal vivo: `DELETE` su un ospite con 2 fatture `PAID` 2026 → `451 LEGAL_HOLD_ACTIVE` con `unlocksAt=2036-08-04`; `DELETE` su un ospite senza storico → `204` invariato (nessuna regressione sul percorso felice). 4 nuovi test boundary-value (2 per il guard sincrono, 2 per il job notturno) | guest-service/client/BillingServiceClient.java, service/impl/GuestServiceImpl.java, service/impl/GuestRetentionJobServiceImpl.java; frontdesk-service/stays/service/impl/StayServiceImpl.java; billing-service/service/impl/InvoiceServiceImpl.java | 4db09fc | A04 | ✅ |
| T-GST-07 | Trovato dal secondo giro di test esplorativo dal vivo (round 1 finding #7 + round 2 bug #6): nessuna `@PreAuthorize` su `GuestController` (delete/export) né su `GuestPrivacySettingsController` (retention policy). Fix: `@PreAuthorize("hasAnyRole('ADMIN','OWNER')")` su `deleteGuest`/`exportGuestData` in `GuestController` e a livello classe su `GuestPrivacySettingsController` (letture e scritture entrambe riservate — la policy di retention è una decisione di configurazione hotel, non un'operazione front-desk). CRUD/ricerca/allegati documento restano aperti a RECEPTIONIST (operatività quotidiana). Verificato dal vivo: RECEPTIONIST → `403` su tutti e 4 gli endpoint, `200` invariato su lettura ospite; nuovi `GuestControllerSecurityTest`/`GuestPrivacySettingsControllerSecurityTest` (`@WebMvcTest` con `SecurityConfig` reale, non lo standalone MockMvc esistente che non intercetta `@PreAuthorize` — stesso pattern di `MenuItemControllerSecurityTest`, T-FB-03) | guest-service/controller/GuestController.java, GuestPrivacySettingsController.java | a87d329 | A01 | ✅ |
| T-BILL-07 | Trovato dal secondo giro di test esplorativo dal vivo (round 2 bug #5): nessuna `@PreAuthorize` su `InvoiceController`. Fix: `@PreAuthorize("hasAnyRole('ADMIN','OWNER')")` su `updateDocumentType`, `getFatturaPAXml`, `updateSdiStatus`, `exportBatch` — le 4 operazioni fiscalmente sensibili identificate dal test. Lasciati aperti a RECEPTIONIST: lettura/ricerca fatture, download PDF cortesia, `createInvoiceForStay`/`addCharge`/`getLastInvoiceDateForGuest` — quest'ultimi 3 sono chiamate interservizio (frontdesk-service/fb-service/guest-service via Feign) che propagano il ruolo dell'utente che ha originato l'azione (check-in, conferma ordine F&B, cancellazione ospite); restringerli a ADMIN/OWNER avrebbe rotto il check-in di un RECEPTIONIST. Verificato dal vivo: RECEPTIONIST → `403` sui 4 endpoint ristretti, `200` invariato su lettura fattura; ADMIN → invariato (nessuna regressione, un tentativo di `document-type` su fattura `PAID` restituisce `409` per un guard di business preesistente, non `403`). Nuovo `InvoiceControllerSecurityTest` (stesso pattern T-FB-03/T-GST-07) | billing-service/controller/InvoiceController.java | a87d329 | A01 | ✅ |
| T-RES-01 | @Version optimistic lock + @Lock(PESSIMISTIC_WRITE) su overlap query + ConflictException HTTP 409 + Flyway V4 version column | reservation-service/Reservation.java, ReservationRepository.java, GlobalExceptionHandler.java, V4__add_version_to_reservations.sql | ca9bf92 | A04 | ✅ |
| T-RES-02 | hotel_id scope su tutte le query reservation (findByIdAndHotelId, findAllByHotelId); InternalAuthFilter propaga X-Auth-Hotel in auth.details; createReservation imposta hotelId dall'auth context | reservation-service/ReservationServiceImpl.java, ReservationRepository.java, InternalAuthFilter.java, FeignHeaderConfig.java | 3e93f49 | A01 | ✅ |
| T-STAY-01 | Set.of("CONFIRMED","PARTIALLY_CHECKED_IN") come allowlist; checkIn() lancia IllegalStateException("INVALID_RESERVATION_STATUS") se stato non ammesso; log WARN [STAY] CHECK_IN_FAILED reason=INVALID_RESERVATION_STATUS; 4 test: shouldRejectCheckInWhenReservationIsCancelled/IsCheckedOut/IsNoShow + shouldAllowCheckInWhenPartiallyCheckedIn | stay-service/StayServiceImpl.java | 250edd0 | A04 | ✅ |
| T-STAY-02 | Structured SLF4J audit log in StayServiceImpl: CHECK_IN_SUCCESS (stayId+reservationId+guestId+roomId), CHECK_IN_FAILED (reason=EXTERNAL_SERVICE_UNAVAILABLE), CHECK_OUT_SUCCESS (stayId+reservationId+roomId), CHECK_OUT_FAILED (reason=INVALID_STATUS e BILLING_NOT_PAID); prefisso [STAY], WARN per fallimenti, INFO per successi | stay-service/StayServiceImpl.java | f2cb417 | A09 | ✅ |
| T-STAY-04 | Gap preesistente (non causato dal consolidamento ADR-001), trovato da review di sicurezza post-merge su PR #17. StayRepository: nuovi findByIdAndHotelId, findByHotelId(hotelId, Pageable), findAllByReservationIdAndHotelId. getAllStays/getStayById/checkOut/getStaysByReservationId ora richiedono hotelId (da StayController.extractHotelId(), stesso meccanismo già usato da checkIn) e ritornano 404 (no enumeration, pattern T-GST-01) per stay/reservation di altri hotel. findAllByReservationId interno (usato solo dalla saga updateReservationGuests con reservationId già fidato) lasciato invariato. 2 nuovi test (getAllStays/getStaysByReservationId non avevano copertura) + test esistenti aggiornati | frontdesk-service/stays/{repository/StayRepository.java, service/StayService.java, service/impl/StayServiceImpl.java, controller/StayController.java} | 92b8b49 | A01 | ✅ |
| T-ROOM-01 | Trovato da QA manuale via browser su frontdesk-service consolidato (Playwright): una camera creata da UI risultava con hotelId tutto-zero invece di quello reale dell'admin, rendendola invisibile/inutilizzabile da ogni endpoint correttamente scoped (es. creazione prenotazione falliva con ROOM_NOT_FOUND nonostante la camera esistesse). Causa doppia: (1) frontend/inventoryService.ts inviava un placeholder hardcoded tutto-zero quando il form non raccoglie hotelId dall'utente (per design — dovrebbe arrivare dalla sessione autenticata); (2) RoomController.createRoom/RoomServiceImpl.createRoom non derivavano mai hotelId da SecurityContextHolder (pattern già usato altrove, es. StayController.checkIn), fidandosi del body; updateRoom scopeva correttamente la lookup ma poi scriveva request.hotelId() sull'entità. Gap collegato: getAllRooms usava findAllByActiveTrue senza filtro hotelId, ritornando camere di tutti gli hotel (stessa classe di T-STAY-04). Fix: RoomService.createRoom/getAllRooms ora richiedono hotelId esplicito (pattern già usato da getRoomById/updateRoom/updateRoomStatus/deleteRoom); nuovo findAllByActiveTrueAndHotelId; updateRoom scrive il parametro hotelId non più request.hotelId(); RoomController passa resolveHotelId(); inventoryService.ts non fabbrica più un hotelId lato client. 3 nuovi test che costruiscono una request con hotelId diverso da quello autenticato e verificano che vinca quest'ultimo | frontdesk-service/rooms/{repository/RoomRepository.java, service/RoomService.java, service/impl/RoomServiceImpl.java, controller/RoomController.java}, frontend/src/services/inventoryService.ts | 01e90ee | A01 | ✅ |
| T-STAY-05 | Trovato da QA manuale del flusso export/invio Alloggiati (#43). StayRepository.findByActualCheckInTimeBetween(start, end) era l'unica query multi-tenant del repository senza parametro hotelId — usata da AlloggiatiReportServiceImpl.buildRows() per i 3 endpoint Alloggiati. Effetto: GET /reports/alloggiati (download txt) non aveva nemmeno @PreAuthorize — qualunque utente autenticato, di qualunque ruolo e qualunque hotel, scaricava nome/cognome/data nascita/documento di tutti gli ospiti arrivati quel giorno in tutti gli hotel del sistema; GET /reports/alloggiati/json richiedeva ADMIN/OWNER ma restituiva comunque dati cross-tenant; POST /reports/alloggiati/submit ("Invia a Questura") sottometteva alla Polizia di Stato, sotto le credenziali PS dell'hotel chiamante, anche gli arrivi di altri hotel — un invio legale con dati mescolati tra tenant diversi, non solo un leak di lettura. Fix: nuova query findByActualCheckInTimeBetweenAndHotelId(start, end, hotelId); AlloggiatiReportService.generateReport/generateJsonReport e AlloggiatiWebSenderService.submitReport richiedono ora hotelId, risolto server-side da StayController.extractHotelId() (endpoint manuali) o da Stay.hotelId (invio automatico al check-in) — mai dal client. Aggiunto @PreAuthorize("hasAnyRole('ADMIN','OWNER')") mancante sul download txt. Nuovi test di regressione in StayControllerSecurityTest per l'endpoint txt (403 RECEPTIONIST, 200 ADMIN) — quel gap di test è esattamente come l'annotazione mancante è passata inosservata. Stessa classe di T-STAY-04, query non coperta in quel passaggio | frontdesk-service/stays/{repository/StayRepository.java, service/AlloggiatiReportService.java, service/AlloggiatiWebSenderService.java, service/impl/AlloggiatiReportServiceImpl.java, service/impl/AlloggiatiWebSenderServiceImpl.java, service/impl/StayServiceImpl.java, controller/StayController.java} | d62d63d | A01 | ✅ |
| T-BILL-01 | hotel_id scope su tutte le query invoice/payment (findByIdAndHotelId, findByHotelId con Pageable, findFirstByReservationIdAndHotelIdOrderByIssueDateDesc); InternalAuthFilter legge X-Auth-Hotel e lo memorizza in auth.details; createInvoiceForStay/addCharge impostano hotelId dall'auth context (ignora valore client); FeignHeaderConfig propaga X-Auth-Hotel. Nota: il vecchio endpoint manuale `POST /api/v1/invoices` (createInvoice) è stato rimosso (mai cablato a UI, dead code) — la mitigazione hotelId-da-auth-context oggi vale per i path realmente in uso (check-in automatico e charge F&B) | billing-service/InvoiceRepository.java, InvoiceServiceImpl.java, PaymentServiceImpl.java, InternalAuthFilter.java, FeignHeaderConfig.java | 4a44eea | A01 | ✅ |
| T-STAY-06 | Trovato durante il design di una regola ArchUnit per l'isolamento multi-tenant (ADR-004), stessa esplorazione che ha portato a T-BILL-04. `StayServiceImpl.getLastCompletedStayForGuest(guestId)` (pre-fill del form check-in per ospiti di ritorno) chiamava `stayRepository.findTopByGuestIdAndStatusOrderByActualCheckInTimeDesc(guestId, CHECKED_OUT)` senza alcun parametro hotelId — l'unico controllo era che il profilo ospite fosse ancora attivo su guest-service, non che il soggiorno restituito appartenesse all'hotel del chiamante. Un receptionist dell'Hotel A poteva quindi, per un guestId noto/enumerato, ottenere l'ultimo soggiorno di quell'ospite anche se avvenuto presso l'Hotel B (numero camera, date, badge stato fattura/Alloggiati). Fix: nuova query `findTopByGuestIdAndHotelIdAndStatusOrderByActualCheckInTimeDesc`; `StayService.getLastCompletedStayForGuest` richiede ora hotelId, risolto da `StayController.extractHotelId()` (stesso pattern di T-STAY-04/05), mai dal client. Pulizia correlata: rimosso anche `StayRepository.findByReservationId` (singolare, dead code, zero chiamanti). Nuovi test `shouldReturnLastCompletedStayScopedToTheAuthenticatedHotel` + `shouldReturnEmptyWhenGuestServiceUnavailableForPreFill` in StayServiceImplTest (endpoint privo di qualunque copertura di regressione in precedenza) | frontdesk-service/stays/{repository/StayRepository.java, service/StayService.java, service/impl/StayServiceImpl.java, controller/StayController.java} | 6e5bc0e | A01 | ✅ |
| T-STAY-07 | `StayRequest.status` (campo client-controllato) veniva scritto senza filtro sull'entità `Stay`; `checkIn()` forzava `CHECKED_IN` solo se il valore sottomesso era `null`/`EXPECTED`, altrimenti lo accettava — un client poteva inviare `status: CHECKED_OUT` e ottenere una stay già "nata" checked-out, saltando il guard `BILLING_NOT_PAID` di `checkOut()` (mai invocato) e lasciando la camera `OCCUPIED` in modo permanente (`RoomServiceImpl` rifiuta di liberarla per altra via). Trovato dall'audit a 10 agenti (Finding #2, CRITICAL). Fix: `checkIn()` ora imposta incondizionatamente `CHECKED_IN`, ignorando qualunque `status` in ingresso dal client. Verificato che nessun test esistente dipendesse dal vecchio comportamento condizionale (`StayServiceImplTest` già costruiva le sue `StayRequest` con `EXPECTED`; il caso `CHECKED_IN` di `StayControllerTest` gira contro uno `StayService` mockato) | frontdesk-service/stays/service/impl/StayServiceImpl.java | 20a89d2 | A04 | ✅ |
| T-ROOM-02 | Trovato durante l'approfondimento del gap noto documentato in GAP-6 ("RoomType non ha colonna hotel_id — cambio di schema, non query mancante"): quel gap era stato inizialmente accettato come limite architetturale del catalogo camere, ma verificando i call-site emergeva che era un IDOR reale e sfruttabile, non solo un'assenza di filtro — stessa classe di T-BILL-04/T-STAY-06 ma con superficie di scrittura (update/delete), non sola lettura. Fix: Flyway V7 aggiunge `hotel_id UUID NOT NULL` a `room_types` (backfill al placeholder esistente, poi NOT NULL), sostituisce `UNIQUE(name)` con `UNIQUE(hotel_id, name)`; `RoomType` entity + `RoomTypeRepository` (`findByIdAndHotelId`, `findAllByHotelId`) + `RoomTypeService`/`RoomTypeServiceImpl` (tutti i 5 metodi richiedono hotelId) + `RoomTypeController.resolveHotelId()` (stesso pattern di `RoomController`) aggiornati; cache Spring ri-namespaced per hotelId (`#hotelId + ':' + #id` e `#hotelId`) per evitare cache poisoning cross-tenant. Fix connesso trovato nello stesso giro: `RoomServiceImpl.createRoom/updateRoom` chiamavano `roomTypeRepository.findById(roomTypeId)` non scoped (consumer del repository, non catturato dalla regola ArchUnit che guarda solo i metodi dichiarati sul repository stesso) — sostituiti con `findByIdAndHotelId`. `RoomTypeRepository` aggiunto a `TENANT_ROOT_REPOSITORIES` in `TenantIsolationArchTest`. Nuovi test: `RoomTypeServiceImplTest` (2 regressioni cross-hotel su get/update), `RoomTypeControllerTest` (SecurityContext hotel-scoped, pattern `RoomControllerTest`), `RoomTypeServiceIntegrationTest` (Testcontainers Postgres reale: due hotel usano lo stesso nome, un hotel non vede/modifica/cancella la room-type di un altro) | frontdesk-service/rooms/{repository/RoomTypeRepository.java, domain/RoomType.java, service/RoomTypeService.java, service/impl/RoomTypeServiceImpl.java, controller/RoomTypeController.java, mapper/RoomTypeMapper.java, service/impl/RoomServiceImpl.java}, V7__add_hotel_id_to_room_types.sql | 121e4d0 | A01 | ✅ |
| T-BILL-04 | Trovato durante il design di una regola ArchUnit per l'isolamento multi-tenant (ADR-004), non da un pentest mirato: `OwnerReportController.getOwnerFinancialReport` non estraeva mai `hotelId` dal SecurityContext, nonostante `@PreAuthorize("hasAnyRole('OWNER','ADMIN')")` — chiamava `InvoiceRepository.findByIssueDateBetween(start,end)`, che non aveva alcun parametro hotelId, aggregando fatturato e restituendo la lista fatture (dati ospite inclusi) di **tutti** gli hotel della piattaforma a qualunque Owner autenticato, nessuna enumerazione di UUID richiesta — il comportamento di default dell'endpoint stesso. Invisibile finché in produzione c'era un solo hotel; immediatamente sfruttabile con multi-tenant. Fix: `OwnerReportController.extractHotelId()` (stesso pattern già usato da `InvoiceController`) risolve l'hotel dal contesto autenticato e lo passa a `OwnerReportService.getFinancialReport(hotelId, startDate, endDate)`; `InvoiceRepository.findByIssueDateBetween` sostituito con `findByHotelIdAndIssueDateBetween(hotelId, start, end)`. Rimosso anche `InvoiceRepository.findByGuestId` (dead code, zero chiamanti, stesso rischio strutturale se mai richiamato). Nuovo test di regressione `shouldScopeQueryToTheAuthenticatedHotelOnly` in OwnerReportServiceImplTest; OwnerReportControllerTest aggiornato con SecurityContext hotel-scoped | billing-service/controller/OwnerReportController.java, service/OwnerReportService.java, service/impl/OwnerReportServiceImpl.java, repository/InvoiceRepository.java | c983946 | A01 | ✅ |
| T-BILL-05 | Font Noto Sans letti e sub-settati dal classpath una sola volta, in campi statici di `ThymeleafPdfTemplateRenderer`, invece che ad ogni `render()`; causa concorrente (GuestClient Feign senza discovery configurata, "No servers available" su ogni chiamata) corretta pinnando l'URL esplicito via `application.config.guest-service-url` in config-service (stesso pattern già in uso per `HotelSettingsClient`/`frontdesk-service-url`). Rete di sicurezza operativa aggiuntiva: `MaxMetaspaceSize` alzato 128m→192m. Verificato dal vivo: 20 generazioni PDF consecutive → 20×200, Metaspace stabile | pdf-template-engine/ThymeleafPdfTemplateRenderer.java, billing-service/client/GuestClient.java, config-service/config/billing-service.yml, docker-compose.yml | c803390 | A04 | ✅ |
| T-FB-02 | Catalogo MenuItem server-side (V2 Flyway); OrderItemRequest accetta menuItemId+quantity, no unitPrice; service risolve prezzi da DB con buildItemsFromCatalog(); totalAmount ricalcolato server-side | fb-service/MenuItem.java, MenuItemRepository.java, V2__add_menu_items.sql, OrderItemRequest.java, RestaurantOrderServiceImpl.java | d7af61c | A04 | ✅ |
| T-CFG-01 | Porta management separata (:8090, non pubblicata) + show-details: when-authorized su tutti i servizi | config-service/resources/config/*.yml, docker-compose.yml, prometheus.yml | b0cf898 | A05 | ✅ |
| T-CFG-02 | Rimosso fallback JWT_SECRET hardcoded da auth-service.yml; POSTGRES_PASSWORD e JWT_SECRET parametrizzati come variabili d'ambiente nel docker-compose; setup script aggiornato per generare entrambi i segreti. Rimosso anche il fallback `bXktMzIt...` da api-gateway/JwtUtil.java (@Value senza default → fail-fast senza JWT_SECRET); aggiunto jwt.secret: ${JWT_SECRET} ad api-gateway.yml | config-service/config/auth-service.yml, docker-compose.yml, setup-hmac-secret.sh/.ps1, api-gateway/util/JwtUtil.java, config-service/config/api-gateway.yml | 91d9484, 6dc6ea4 | A02 | ✅ |
| T-GW-02 | Rate limiting esteso a tutte le 7 route dell'api-gateway (guest, inventory, reservation, stay, billing, fb, auth): business routes con userKeyResolver (per-user bucket "user:<username>", burst 50, 20 req/s); auth route con remoteAddrKeyResolver proxy-aware (X-Forwarded-For first, fallback RemoteAddress, burst 10, 5 req/s); AuthenticationFilter dichiarato prima di RequestRateLimiter per esporre X-Auth-User al resolver | api-gateway/RateLimiterConfig.java, config-service/config/api-gateway.yml | 25c7b57 | A05 | ✅ |
| T-RES-03 | @ValidDateRange class-level Bean Validation constraint su ReservationRequest: checkOutDate.isAfter(checkInDate) obbligatorio (rifiuta zero-night stay e date invertite); DateRangeValidator implementa ConstraintValidator; guard difesa-in-profondità verifyDateRange() in ReservationServiceImpl.createReservation() e updateReservation() lancia BadRequestException("CHECKOUT_MUST_BE_AFTER_CHECKIN") prima di qualsiasi accesso a servizi esterni o DB; 6 nuovi test (3 service + 3 validator + null-safety) | reservation-service/ReservationRequest.java, ValidDateRange.java, DateRangeValidator.java, ReservationServiceImpl.java | 43be320 | A03 | ✅ |
| T-STAY-03 | AlloggiatiWebConfig: RestTemplate con JVM default SSL context (no TrustAllCerts, no hostname-verifier bypass); AlloggiatiWebSenderServiceImpl: credenziali lette da env vars (ALLOGGIATI_USERNAME, ALLOGGIATI_PASSWORD), mai hardcoded; Basic Auth header RFC 7617; 5 test con MockRestServiceServer (POST URL, Authorization header, Content-Type, 5xx → ExternalServiceException, empty report) | stay-service/AlloggiatiWebConfig.java, AlloggiatiWebSenderService.java, AlloggiatiWebSenderServiceImpl.java, config-service/stay-service.yml | f0be747 | A02 | ✅ |
| T-BILL-02 | @Digits(integer=10, fraction=2) su PaymentRequest.amount e InvoiceRequest.totalAmount; @Size(max=100)+@Pattern su transactionReference; PaymentServiceImpl.addPayment() normalizza l'importo con setScale(2, HALF_UP) prima di ogni aritmetica; payment.setAmount(paymentAmount) assicura valore normalizzato persistito; 2 nuovi test: shouldThrowWhenInvoiceAlreadyPaid + shouldNormalizeAmountToTwoDecimalPlaces | billing-service/PaymentRequest.java, InvoiceRequest.java, PaymentServiceImpl.java | d346fe8 | A04 | ✅ |
| T-FB-01 | hotel_id scope su restaurant_orders (Flyway V3); InternalAuthFilter legge X-Auth-Hotel e lo memorizza in auth.details; createOrder imposta hotelId dall'auth context; getOrdersByStayId usa findByStayIdAndHotelId; getAllOrders usa findAllByHotelId(pageable) — nessun ordine cross-hotel mai restituito; FeignHeaderConfig propaga X-Auth-Hotel; Flyway V3 fix anche CHECK constraint status enum | fb-service/RestaurantOrder.java, RestaurantOrderRepository.java, RestaurantOrderServiceImpl.java, InternalAuthFilter.java, FeignHeaderConfig.java, V3__add_hotel_id_to_restaurant_orders.sql | 18a3768 | A01 | ✅ |
| T-CFG-03 | HTTP Basic Auth su config-service (SecurityConfig + spring.security.user); credenziali CONFIG_SERVER_USERNAME (default configuser) e CONFIG_SERVER_PASSWORD iniettate via env var; tutti gli 8 microservizi consumer aggiungono spring.cloud.config.username/password in application.yml; CONFIG_SERVER_PASSWORD generato da setup-hmac-secret.sh/.ps1 e iniettato nel docker-compose; 2 test di integrazione (401 senza credenziali, 200 con credenziali valide) | config-service/SecurityConfig.java, application.yml, tutti i microservizi/application.yml, docker-compose.yml, setup-hmac-secret.sh/.ps1 | c934f6e | A07 | ✅ |
| T-FE-01 | Audit completo: nessun dangerouslySetInnerHTML trovato; React JSX escapa l'output per default. Enforcement statico: ESLint no-restricted-syntax vieta dangerouslySetInnerHTML, innerHTML e outerHTML diretti (errore di build). Test di regressione: Guests.test.tsx verifica che payload XSS (`<img src=x onerror=alert(1)>`) in dati API sia escaped come testo e non injettato come elemento DOM | frontend/eslint.config.js, frontend/src/pages/Guests.test.tsx | 3655906 | A03 | ✅ |
| T-FE-03 | ProtectedRoute con prop allowedRoles?: Role[]; se fornito, utenti con ruolo non in allowlist → redirect a /. App.tsx: /owner-dashboard annidato in <ProtectedRoute allowedRoles={['OWNER','ADMIN']}> dentro MainLayout. 4 nuovi test: ADMIN/OWNER ammessi, RECEPTIONIST → redirect /, user null → redirect /. 144/144 test verdi, lint zero warnings, tsc clean | frontend/src/components/ProtectedRoute.tsx, frontend/src/App.tsx, frontend/src/components/ProtectedRoute.test.tsx | ccfa431 | A01 | ✅ |
| T-FE-03b | Estensione pattern "role-gated service call": dashboardService.getDashboardStats(isOwnerOrAdmin) — la chiamata a /api/v1/reports/owner (@PreAuthorize OWNER\|ADMIN) era eseguita incondizionatamente per tutti i ruoli, restituendo 403 per RECEPTIONIST/MANAGER e rompendo l'intera dashboard. Fix: parametro isOwnerOrAdmin propagato da dashboardStore.fetchStats() a Dashboard.tsx; pendingRevenue=null se false, chiamata billing omessa. Previene chiamate API non autorizzate a livello di servizio frontend (complementare a ProtectedRoute che opera a livello di route). | frontend/src/services/dashboardService.ts, frontend/src/store/dashboardStore.ts, frontend/src/pages/Dashboard.tsx | 14a43bb | A01 | ✅ |
| T-FE-04 | CSP SPA-level via nginx.conf: script-src 'self', style-src 'self' 'unsafe-inline', connect-src 'self', frame-ancestors 'none'; nginx proxy /api/ → api-gateway per same-origin; api.ts baseURL relativo | frontend/nginx.conf, frontend/Dockerfile, frontend/src/services/api.ts | b901a9f | A05 | ✅ |
| GAP-2 | E2E attack-path coverage: 11 Playwright test in 3 suite. security-auth.spec.ts: T-AUTH-SEC-01 (401 unauthenticated→/login), T-AUTH-SEC-02 (401+refresh fail→performLogout→/login), T-AUTH-SEC-03 (429 brute-force→generic error, no account detail), T-AUTH-SEC-04 (403 CSRF→toast error, modal aperto). security-rbac.spec.ts: T-RBAC-SEC-01 (RECEPTIONIST→/owner-dashboard→redirect /), T-RBAC-SEC-02/03 (ADMIN/OWNER→accesso concesso), T-RBAC-SEC-04 (unauthenticated→/billing→/login). security-idor.spec.ts: T-IDOR-SEC-01 (guest edit cross-hotel 404→toast error), T-IDOR-SEC-02 (URL manipulation reservation 404→error state), T-IDOR-SEC-03 (guest list hotel-scoped). | frontend/e2e/security-auth.spec.ts, security-rbac.spec.ts, security-idor.spec.ts | f2cbc7d | A01, A07 | ✅ |
| GAP-3 | fb-service InternalAuthFilter struttura inconsistente: il presence-check usava due `if` separati (il primo controllava username/role/signature ma non hotelId; il secondo controllava hotelId con LOG.warn dedicato) invece del singolo `if` combinato usato dagli altri 5 servizi. Fix: unificato in un singolo `if` con quattro condizioni (||) identico al pattern canonico; aggiornato Javadoc (tre→quattro header obbligatori); rimosso LOG.warn superfluo per assenza header (il HMAC-mismatch log rimane). Aggiunti 8 test JUnit 5 in InternalAuthFilterTest: 4 presenza (username/role/hotelId/signature mancante→401), 2 HMAC (firma invalida→401, firma per hotelId diverso→401 tenant isolation), 1 valido (200 + SecurityContext popolato con name/details/authority), 1 actuator bypass (shouldNotFilter→200 senza headers). | fb-service/src/main/java/com/hotelpms/fb/security/InternalAuthFilter.java, fb-service/src/test/java/com/hotelpms/fb/security/InternalAuthFilterTest.java | 9c299b1 | A01 | ✅ |
| GAP-6 | Controllo preventivo, non un singolo threat: T-BILL-04 e T-STAY-06 sono stati trovati manualmente durante l'esplorazione per ADR-004 (decisione multi-tenant) — nessun meccanismo impediva che una futura query repository su un'entità tenant-root (con colonna `hotel_id` propria) venisse scritta senza scoping hotelId, lasciando la scoperta di questa classe di bug interamente alla revisione umana. Fix: `TenantIsolationArchTest` (ArchUnit) aggiunto a tutti e 5 i servizi con repository multi-tenant (auth, guest, frontdesk, billing, fb) — fallisce la build se un metodo custom su un repository tenant-root non contiene `HotelId` nel nome e non è annotato `@TenantScopeExempt(reason=...)`. I metodi ereditati da JpaRepository/CrudRepository (findById, save, delete) sono fuori scope (guardati al call-site, pattern findByIdAndHotelId già in uso). 8 eccezioni documentate e verificate manualmente (non semplicemente silenziate): login/registrazione pre-hotel-resolve su UserAccount (4, auth-service), batch GDPR notturno platform-wide su Guest (1, guest-service), query overlap prenotazioni con roomIds pre-scoped a monte da verifyRoomsAvailability/findCleanRooms (3, frontdesk-service). billing-service e fb-service non necessitano eccezioni. Rimosso anche codice morto trovato nella stessa esplorazione (Room.findByRoomNumberAndActiveTrue, Room.findAllByActiveTrue, Invoice.findByGuestId, Stay.findByReservationId — zero chiamanti). Verificato che la regola cattura davvero violazioni (smoke test: metodo temporaneo non-scoped aggiunto/rimosso, confermato fallimento). Gap noto all'epoca e accettato come limite architetturale (`RoomType` senza colonna `hotel_id`, catalogo ritenuto globale) — approfondito successivamente e risultato un IDOR reale e sfruttabile: vedi T-ROOM-02. **Estensione 2026-08 (audit a 10 agenti)**: `QuotationRepository` (entità aggiunta dopo l'introduzione della regola) non era mai stata inserita in `TENANT_ROOT_REPOSITORIES` — nessuna violazione reale trovata (i suoi due metodi custom, `findAllByHotelId`/`findByIdAndHotelId`, erano già correttamente scoped), ma la regola non la stava verificando. Aggiunta alla lista, test rieseguito e confermato verde contro il codice reale | auth/guest/frontdesk/billing/fb-service/architecture/{TenantScopeExempt.java, TenantIsolationArchTest.java}, */build.gradle.kts | 797d676, 509eb7a | A01 | ✅ |
| T-NOTIF-01 | notification-service era già protetto da `InternalAuthFilter` (stesso meccanismo HMAC+anti-replay di T-GW-08) fin dalla sua introduzione, ma non era mai stato censito in questo documento — asset citato solo in tabella §1, zero threat/mitigazione tracciati. Formalizzato durante l'estrazione di `internal-auth-lib` (AUDIT_ANALISI_2026-07.md item 2), che ha portato a riesaminare la protezione di tutti e 6 i servizi insieme | notification-service/security/{InternalAuthFilter.java, SecurityConfig.java} (ora internal-auth-lib) | 5d13a7d, 9cec99f | A01, A07 | ✅ |
| GAP-7 | `InternalAuthFilter`/`NonceStore`/`RedisNonceStore` esistevano come 6 copie quasi-identiche (auth/billing/fb/frontdesk/guest/notification-service): 5 byte-identiche per puro accidente di copia-incolla disciplinata, non per un meccanismo che lo garantisse — nessun controllo impediva a una futura modifica di introdurre una sesta divergenza silenziosa. auth-service era già la prova che questo rischio non era ipotetico: allowlist invertita invece di denylist, controllo presenza header con `== null` invece di `StringUtils.hasText` (un header `X-Auth-User: ""` non veniva rifiutato), contratto 401 diverso (`sendError`+codice stringa invece di corpo JSON), zero logging sui rigetti (nessuna riga WARN su HMAC invalido/nonce riusato, a differenza degli altri 5) — tre di queste sono difetti di sicurezza reali (bypass presenza header, assenza di log per il rilevamento di attacchi), scoperti solo grazie a questo lavoro di dedup, non da un pentest mirato. Fix: characterization test per fissare il comportamento reale di ciascun servizio prima di toccare nulla; riconciliazione di auth-service sul contratto condiviso (StringUtils.hasText, corpo JSON, logging sanitizzato CWE-117, `excludedPathPrefixes` esplicito invece di logica invertita hardcoded); estrazione in `internal-auth-lib` (nuovo modulo Gradle, libreria pura senza autoconfigurazione Spring) — un solo punto di validazione HMAC per tutti e 6 i servizi, non sei. Verificato dal vivo su tutti e 6 i servizi (richiesta non firmata→401, timestamp scaduto→401, nonce riusato→401, `/actuator/health/liveness` senza header→200) più un flusso applicativo autenticato completo via browser attraverso il gateway reale, nessuna regressione | auth/billing/fb/frontdesk/guest/notification-service (security package), nuovo internal-auth-lib | 5d13a7d, 8607d7c, 9cec99f | A01, A04 | ✅ |
| T-FB-03 | `SecurityConfig` di fb-service mancava `@EnableMethodSecurity` — i 3 `@PreAuthorize("hasAnyRole('ADMIN','OWNER')")` su `MenuItemController` (create/update/delete) non venivano mai valutati da Spring: qualunque ruolo autenticato, incluso RECEPTIONIST, poteva creare, modificare o cancellare voci di menu. Trovato non da un pentest ma diffando le 5 copie di `SecurityConfig` prima di consolidarle (vedi GAP-8) — l'assenza dell'annotazione era invisibile leggendo il singolo file, evidente solo confrontandolo con gli altri 4 servizi. Fix: `@EnableMethodSecurity` aggiunta a fb-service; applicata anche a guest-service (che non ha ancora nessun `@PreAuthorize`) come difesa in profondità, così questa classe di bug non può ripresentarsi silenziosamente. Nuovo `MenuItemControllerSecurityTest` (`@WebMvcTest` + `SecurityConfig` reale, non uno standalone MockMvc che avrebbe bypassato la filter chain e non l'avrebbe mai intercettato — esattamente come il `MenuItemControllerTest` esistente non l'ha mai intercettato) | fb-service/security/SecurityConfig.java, controller/MenuItemController.java, guest-service/security/SecurityConfig.java, nuovo fb-service/test/controller/MenuItemControllerSecurityTest.java | bdce1e5 | A01 | ✅ |
| GAP-8 | `FeignHeaderConfig` (4 copie, 107-125 righe) e `SecurityConfig` (5 copie, 75-79 righe) presentavano lo stesso pattern di rischio già visto in GAP-7: duplicazione mantenuta identica per disciplina, non per garanzia strutturale. Diffando le copie prima di consolidarle sono emersi due difetti reali, non cosmetici: (1) T-FB-03 sopra; (2) né fb-service né billing-service gestivano `AccessDeniedException` nel proprio `GlobalExceptionHandler` — un diniego `@PreAuthorize` (corretto, non un bypass: l'autorizzazione era comunque negata) cadeva nell'handler generico e tornava 500 invece di 403, un contratto di risposta scorretto che billing-service aveva da quando `OwnerReportController` ha adottato `@PreAuthorize`, mai emerso perché il traffico non aveva mai esercitato il path di diniego in modo osservabile. Fix: `FeignHeaderConfig` → `InternalFeignAuthInterceptor` in internal-auth-lib (guest-service ha un fallback reale, il job GDPR batch fuori da contesto HTTP, modellato come `FeignAuthFallbackProvider` invece di un `if` duplicato); `SecurityConfig` → `InternalApiSecurityFilterChainFactory`, stessa libreria. Handler `AccessDeniedException` aggiunto a fb-service e billing-service (pattern già presente in frontdesk-service). Nello stesso giro, anche `GlobalExceptionHandler` (6 copie): solo billing/fb/guest erano davvero identici (stesso host URI, stessa forma degli errori di validazione) e sono stati consolidati in `AbstractProblemDetailAdvice` (nuovo modulo `common-web-lib`); auth-service e frontdesk-service usano una forma diversa per gli errori di validazione (mappa campo→messaggio, non lista) e frontdesk-service genera un `traceId` che gli altri non hanno — forzarli nella stessa base avrebbe significato perdere il traceId o cambiare silenziosamente il contratto di risposta, quindi sono rimasti separati. Trovati invece 5 host URI diversi per il campo `type` RFC 7807 (`hotel-pms.com`, `hotelpms.com`, `api.hotelpms.com`, `hotelpms.local/problems`) — nessuno è un dominio registrato (progetto pre-lancio, vedi `backup/DECISIONS.md`), nessun frontend o test e2e dipende dal valore esatto: normalizzati tutti su `https://hotel-pms.com/errors/`. Verificato con build completa (`./gradlew clean build`, 193 task, zero warning PMD/checkstyle/SpotBugs) più i test di regressione esistenti su ciascun servizio toccato | internal-auth-lib (feign/, security/), nuovo common-web-lib, billing/fb/guest/auth/frontdesk/notification-service (exception/security package) | 79cd3b1, bdce1e5, 54575ec | A01, A05 | ✅ |
| T-OPS-01 | RPO fisso a 24h (`db-backup` container, `pg_dumpall` giornaliero pianificato): un disastro del volume Postgres poco prima del prossimo dump pianificato perdeva fino a un'intera giornata di lavoro (prenotazioni, check-in, fatture). Decisione originale (`backup/DECISIONS.md §3.5`, 2026-07-30) aveva accettato esplicitamente questo RPO rifiutando PITR per complessità non giustificata — riaperta e ribaltata il 2026-08-01 su richiesta diretta dell'utente, non da uno dei criteri di revisione documentati. Fix: sostituito interamente `docker/db-backup/` con **pgBackRest** integrato nel container `postgres` (`docker/postgres/`) — WAL archiving continuo (`archive_timeout=120s`, RPO in minuti) più backup full/incrementali schedulati su due repository indipendenti (locale + Backblaze B2 off-site). Verificato dal vivo: WAL archiving confermato su entrambi i repo (`pg_switch_wal()` forzato, segmento su repo1 e repo2 entro pochi secondi); **drill PITR reale** — tabella marker creata, riga inserita, `pgbackrest restore --type=time` puntato a un istante precedente l'insert su directory scratch separata dallo stack live, avviato come istanza Postgres temporanea — la riga marker risulta assente, la tabella (creata prima del target) presente, `flyway_schema_history` coerente; log pgBackRest conferma *"recovery stopping before commit of transaction ..."* alla transazione esatta dell'insert. Bug trovato e corretto nello stesso giro: la prima versione dello scheduler backuppava solo il repo di default (repo1) — `pgbackrest backup` opera un repo per invocazione, a differenza di `archive-push` che scrive su tutti i repo configurati automaticamente; `backup-scheduler.sh` ora itera esplicitamente su ogni repo. Nuovo `.github/workflows/backup-restore-drill.yml` (drill off-site settimanale su runner GitHub effimero, non ancora eseguito dal vivo — richiede secret CI) | docker/postgres/{Dockerfile, entrypoint-wrapper.sh, backup-scheduler.sh}, docker-compose.yml, `.github/workflows/backup-restore-drill.yml`, rimosso docker/db-backup/ | 48828ad, 663a0bf | A09 | ✅ |
| T-OPS-02 | Cambio del modello di cifratura backup da asimmetrico (`age`, solo chiave pubblica sull'host di produzione, la privata mai) a simmetrico (pgBackRest `repo-cipher-pass`) — necessario per il WAL archiving continuo di T-OPS-01: la passphrase deve essere presente in tempo reale sullo stesso host che fa girare Postgres. Compromettere l'host di produzione oggi espone anche la chiave per decifrare i backup passati e futuri, proprietà che lo schema `age` precedente escludeva esplicitamente per design. Rischio accettato esplicitamente, non scoperto per caso: trade-off dichiarato all'utente prima dell'implementazione (in cambio di WAL archiving continuo, verify/restore-check integrati, meno codice custom hand-rolled — coerente con `ADR-002`), documentato per iscritto in `backup/DECISIONS.md §3.5b` prima di procedere | docker/postgres/entrypoint-wrapper.sh (generazione `pgbackrest.conf`), `.env.example` (`PGBACKREST_CIPHER_PASS`) | 48828ad | A02 | ⚠️ RISCHIO ACCETTATO |
| GAP-4 | Audit log non aggregati (SIEM assente): i log strutturati [AUTH], [STAY], [BILLING] venivano emessi solo su stdout senza possibilità di aggregazione, ricerca e alerting su anomalie. Fix: (1) Loki (grafana/loki:2.9.0) + Grafana (grafana/grafana:10.2.0) aggiunti al docker-compose con network observability-network, volumi persistenti loki_data/grafana_data e healthcheck; (2) loki-logback-appender:1.5.2 aggiunto come dipendenza runtime a auth-service, stay-service, billing-service; (3) logback-spring.xml creato per ciascuno dei tre servizi: ConsoleAppender sempre attivo + Loki4jAppender (HTTP push, label service/level, AsyncAppender neverBlock=true) attivo solo nel profilo Docker (auth-service/stay-service/billing-service) — in test e sviluppo locale solo console; (4) Grafana provisioning automatico: datasource Loki + dashboard "Security Audit Log" con 6 pannelli (LOGIN_FAILED/h, ACCOUNT_LOCKED/h, HMAC Failures/h, trend eventi/min, log raw [AUTH]/[STAY]/[BILLING], tabella eventi recenti). | auth-service/logback-spring.xml, stay-service/logback-spring.xml, billing-service/logback-spring.xml, docker-compose.yml, docker/loki/loki-config.yaml, docker/grafana/provisioning/ | fe99e2b | A09 | ✅ |

| DEP-CVE-01 | Bump Netty 4.1.130→4.1.132.Final (CVE-2026-33870 request smuggling + CVE-2026-33871 HTTP/2 DoS) e Spring Cloud Gateway 4.2.0→4.2.6 (CVE-2025-41235 header forwarding + CVE-2025-41253 EL injection): ext["netty.version"]="4.1.132.Final" + dependencyManagement override gateway-server:4.2.6 in api-gateway/build.gradle.kts | api-gateway/build.gradle.kts | 14d29b9 | A06 | ✅ |
| DEP-CVE-02 | Bump commons-fileupload 1.5→1.6.0 (CVE-2025-48976 DoS via part headers) + commons-io 2.11.0→2.14.0 (CVE-2024-47554 XmlStreamReader DoS) in tutti gli 8 microservizi (auth, billing, config, fb, guest, inventory, reservation, stay). Fix corretto via dependencyManagement { dependencies { dependency(...) } } — la tecnica ext["commons-fileupload.version"] non funzionava perché commons-fileupload NON è gestito dal Spring Boot 3.4.x BOM (rimosso assieme a CommonsMultipartResolver in Spring 6.1). CI SCA (OWASP Dependency Check) rimosso perché instabile (NVD API in manutenzione, run >1h), sostituito dalla copertura Trivy per A06. | */build.gradle.kts | e9b6eb6 | A06 | ✅ |
| DEP-CVE-03 | CVE-2026-22739 — Spring Cloud Config Server 4.2.0: path traversal via profile parameter, consente lettura di file arbitrari dal filesystem. Fix upstream richiede config-server 4.3.2 (Spring Cloud 2025.x) o 5.0.2, incompatibili con Spring Boot 3.4.x/Spring Cloud 2024.0.x. Rischio residuale mitigato da: (1) config-service non esposto all'esterno (non instradato dall'API gateway), (2) accesso limitato alla rete interna Docker, (3) nessun file sensibile presente nel classpath del container oltre ai config YAML. Soppresso in .trivyignore con giustificazione. Migrazione pianificata al prossimo aggiornamento Spring Boot 3.5.x. | .trivyignore, config-service/build.gradle.kts | e9b6eb6 | A06 | ✅ |
| DEP-CVE-04 | Upgrade Spring Boot 3.4.x → 3.5.x e Spring Cloud 2024.0.x → 2025.0.x: risolve CVE-2026-22739 (config-server path traversal, fix upstream finalmente disponibile), CVE Netty serie 4.1.13x (request smuggling / HTTP/2 DoS). CVE-2026-42577 (Netty native epoll) classificato accepted risk: Spring Boot non attiva il trasporto native epoll in configurazione JDK NIO standard — rischio pratico nullo. 108 alert Trivy su immagini di terze parti (Grafana, postgres, Redis) analizzati e triaggiati con motivazione tecnica documentata in .trivyignore. | */build.gradle.kts, .trivyignore, docker-compose.yml | dc49c79 | A06 | ✅ |
| DEP-CVE-05 | Upgrade react-router-dom + react-router 7.13.1 → 7.17.0: risolve 6 CVE — XSS via RSC redirect (#36), XSS via Location header (#37), RCE via turbo-stream (#38), open redirect (#39), DoS via __manifest (#40), DoS via single-fetch (#41). Upgrade esbuild 0.27.3 → 0.28.1 via npm overrides (dipendenza transitiva di Vite): risolve CVE-2025-42577 (arbitrary file read via dev server su Windows). Tutti e 7 i fix applicati tramite npm install senza breaking change; 344/344 test verdi, lint zero warnings. | frontend/package.json, frontend/package-lock.json | b1be3e8 | A06 | ✅ |
| DEP-CVE-06 | GHSA-qwww-vcr4-c8h2 (Dependabot #58, HIGH): react-router CSRF bypass in unstable RSC-mode action code paths. **Rischio accettato il 2026-07-31**: l'advisory si applica solo a chi usa le API RSC unstable — verificato via grep che questo codebase non le usa affatto (`BrowserRouter` classico, zero import RSC); al tempo il fix in `react-router-dom` 7.x non esisteva ancora (serie 7 ferma a 7.18.1, il changelog indicava 8.3.0 come primo fix, una migrazione major non giustificata per un rischio non sfruttabile). **Fix 2026-08-10 (Dependabot #68, stesso GHSA)**: upstream ha backportato il fix sulla serie 7.x con `react-router-dom@7.18.2` — bump applicato (`^7.17.0` in `package.json` già lo consentiva, solo il lockfile è cambiato), nessuna migrazione a v8 necessaria. | frontend/package-lock.json | 8fc9ddb | A06 | ✅ |
| DEP-CVE-07 | ~25 alert CodeQL/Trivy sul binario `grafana/grafana:12.4.6` (CVE-2026-39822, CVE-2026-27145, GHSA-hrxh-6v49-42gf e altri). Grafana bumpato a `13.1.1` (ultima patch dell'ultima major disponibile) invece di triaggiare ogni CVE singolarmente — un'immagine di terze parti non offre altra leva se non la versione. Verificato dal vivo: `/api/health` riporta `13.1.1`, datasource Loki e dashboard "Hotel PMS — Security Audit Log" (GAP-4) sopravvivono all'upgrade di major version senza reprovisioning. `.trivyignore` aggiornato (riferimento versione stale corretto); le soppressioni Go-stdlib restano fino alla prossima scansione CI, che risolverà automaticamente quelle già chiuse dalla nuova immagine. **Fix 2026-08-10**: gli stessi ~25 alert Trivy erano ricomparsi nella CI (`code-scanning/alerts`, tutti su `usr/share/grafana/bin/grafana*`) perché `.github/workflows/ci.yml` (job `trivy-thirdparty`) aveva ancora il tag `grafana/grafana:12.4.6` pinnato a mano, mai allineato al bump di `docker-compose.yml` — drift tra i due file, non una regressione della release Grafana. Tag CI allineato a `13.1.1`. | docker-compose.yml, .trivyignore, .github/workflows/ci.yml | 854b8b7, 8fc9ddb | A06 | ✅ |
| GAP-9 | CodeQL (`java/spring-disabled-csrf-protection`, HIGH) segnalava `csrf(AbstractHttpConfigurer::disable)` in `InternalApiSecurityFilterChainFactory` (internal-auth-lib, condivisa da billing/fb/frontdesk/guest/notification-service) come CSRF disabilitato. **Falso positivo confermato**, non un fix: questa filter chain protegge solo chiamate interne service-to-service dietro il gateway, mai raggiungibili da browser — `sessionCreationPolicy(STATELESS)` (nessun cookie di sessione, il vettore che la protezione CSRF esiste per contrastare) e autenticazione via firma HMAC (`InternalAuthFilter`, stesso meccanismo di T-GW-08) che una pagina malevola non può forgiare senza il secret condiviso. Il traffico browser-facing reale ha una difesa CSRF distinta e corretta (`CsrfFilter` sul gateway, T-GW-05, double-submit cookie). Verificato con revisione indipendente (agente `security-auditor`, non solo lettura del commento): confermato che nessuno dei 5 servizi espone questi endpoint direttamente a un browser e che non esiste alcuna credenziale auto-allegata sfruttabile su questo percorso. Aggiunto commento di soppressione `codeql[...]` inline (stesso pattern già usato per il falso positivo log-injection di GAP-7) e alert dismissato su GitHub con motivazione. | internal-auth-lib/src/main/java/com/hotelpms/internalauth/security/InternalApiSecurityFilterChainFactory.java | f8c139a | A01 | ✅ |
| GAP-10 | `frontend/nginx.conf`: la location `/api/` non dichiarava un proprio blocco `add_header`, quindi ereditava silenziosamente `X-Frame-Options: DENY` e la CSP `frame-ancestors 'none'` dal server block (T-FE-04/T-GW-03) — stesso bug di ereditarietà `add_header` di nginx già documentato nel commento del file per le location `/` e `/assets/`, mai esteso a `/api/`. Non solo un gap di hardening: fattura PDF, FatturaPA XML e preventivo PDF si scaricano tutti via `<iframe src="/api/...">` nascosto same-origin, e un `DENY` sulla risposta fa sì che il browser rifiuti di caricarla nell'iframe — ogni download falliva silenziosamente, nessun errore visibile, nessun file. Scoperto da un audit dal vivo (click reali sul bottone "Scarica PDF", non solo apertura di modal o test unitari — questi ultimi mockano l'iframe e non passano mai da nginx reale) innescato da una segnalazione diretta dell'utente. Riprodotto isolando la causa: `curl` e `fetch()` diretti sullo stesso URL restituivano sempre 200 con `Content-Disposition` corretto, solo il caricamento reale nell'iframe falliva. Fix: `/api/` dichiara ora i propri header, `X-Frame-Options: SAMEORIGIN` e `frame-ancestors 'self'` al posto di `DENY`/`'none'` — permette l'iframe same-origin che l'app usa deliberatamente, blocca comunque l'embedding da un'origine esterna (la minaccia reale che l'header protegge). Verificato dal vivo dopo rebuild+redeploy: un vero click su "Scarica PDF" produce il file su disco (25149 byte, corrispondenti alla dimensione generata dal backend). | frontend/nginx.conf | 41bedde | A05 | ✅ |
| GAP-11 | XXE hardening mancante su `FatturaPAServiceImpl` (billing-service): `DocumentBuilderFactory` (generazione XML FatturaPA) e `TransformerFactory` (serializzazione) non disabilitavano DOCTYPE/entità esterne. Trovato da uno scan Semgrep mirato su injection/XXE (595 regole Java+OWASP), non da un pentest — **non sfruttabile allo stato attuale**: il factory costruisce solo un `Document` vuoto in memoria (mai un parsing di input esterno) e il transformer serializza quel DOM, mai uno stylesheet esterno. Difesa in profondità, stesso standard già applicato in `AlloggiatiWebSenderServiceImpl` (T-STAY-03 adiacente): `disallow-doctype-decl` sul factory, `ACCESS_EXTERNAL_DTD`/`ACCESS_EXTERNAL_STYLESHEET` vuoti sul transformer. Zero cambio di comportamento (nessun parsing di DOCTYPE/stylesheet avveniva già) | billing-service/service/impl/FatturaPAServiceImpl.java | 3fc4b07 | A05 | ✅ |
| GAP-12 | `PropertyReferenceException` (sort su property inesistente in un endpoint paginato, es. `?sort=nonEsiste`) non gestita in nessuno dei 6 `GlobalExceptionHandler` — cadeva nel catch-all generico e tornava 500 invece di 400. Spring Data valida il path contro il metamodello JPA **prima** di generare SQL (non è injection, verificato durante lo stesso audit Semgrep di GAP-11), ma è lo stesso pattern di bug già chiuso con T-AUTH-10 (eccezione client-side mascherata da errore server). Fix: nuovo handler condiviso in `AbstractProblemDetailAdvice`, ereditato automaticamente da tutti e 6 i servizi — nessuna modifica ai `GlobalExceptionHandler` per-servizio | common-web-lib/exception/AbstractProblemDetailAdvice.java | 3fc4b07 | A05 | ✅ |
| GAP-13 | `fb-service/application-fb-service.yml` (config locale) esponeva Actuator per intero (`include: "*"`, `show-details: always` — incluso `/actuator/env`, `/actuator/heapdump`) mentre `config-service` serve già per lo stesso servizio la configurazione corretta (`prometheus,health,info` + `when-authorized`, identica agli altri 6 servizi) — unico dei 4 file `application-<service>.yml` esistenti a toccare la config actuator, residuo locale mai allineato. Trovato dallo stesso scan Semgrep (regola `spring-actuator-fully-enabled-yaml`). Fix: allineato al valore già corretto in config-service, a prescindere da quale property source vince a runtime | fb-service/src/main/resources/application-fb-service.yml | c2992c2 | A05 | ✅ |
| GAP-14 | `frontend/Dockerfile`: nginx (master process) girava come root — nessun `USER` dichiarato, immagine base `nginx:alpine` standard. Trovato dallo stesso scan Semgrep (regola Dockerfile `missing-user`). Fix: swap a `nginxinc/nginx-unprivileged:alpine` (immagine ufficiale, ascolto interno su 8080 — le porte <1024 richiedono root per il bind — con `USER nginx` esplicito), `frontend/nginx.conf` aggiornato (`listen 8080`), `docker-compose.yml` porte `"80:8080"` (porta host resta 80, nessun impatto esterno/CI). Verificato dal vivo: `docker compose up -d frontend`, healthcheck interno su :8080 healthy, `curl` dall'host su :80 risponde 200 invariato | frontend/Dockerfile, frontend/nginx.conf, docker-compose.yml | a4df5c2 | A05 | ✅ |
| GAP-15 | `.github/workflows/ci.yml`: 7 GitHub Action referenziate per tag mutabile (`actions/checkout@v7`, `setup-java@v5`, `cache@v6`, `upload-artifact@v7`, `setup-node@v7`, `download-artifact@v8`, `codeql-action/upload-sarif@v4`) — un tag può essere ripuntato dal maintainer dell'action senza preavviso (supply-chain, vedi incidente reale `tj-actions/changed-files` 2025). Il progetto pinnava già `aquasecurity/trivy-action` e `docker/setup-buildx-action` a SHA — le altre 7 erano rimaste sul pattern precedente. `.github/dependabot.yml`: nessun `cooldown` period su alcuno degli 11 blocchi `package-ecosystem` — una dipendenza appena pubblicata (potenzialmente compromessa, tipico pattern di attacco: pubblicare una versione malevola e sperare in un merge automatico prima che venga rimossa) veniva proposta in PR immediatamente. Trovato dallo stesso scan Semgrep (`github-actions-mutable-action-tag`, `dependabot-missing-cooldown`). Fix: le 7 action pinnate a SHA (stesso pattern `@<sha40> # vX.Y.Z` già in uso, SHA risolti dal tag corrente — zero cambio di comportamento, stessi binari); `cooldown: default-days: 7` aggiunto a tutti gli 11 blocchi Dependabot. Dependabot continua a proporre bump aggiornando SHA+commento insieme, nessuna configurazione aggiuntiva richiesta | .github/workflows/ci.yml, .github/dependabot.yml | 0cfbe60 | A08 | ✅ |
| GAP-16 | `docker/postgres/Dockerfile` non dichiara `USER` (root) — stessa regola Semgrep di GAP-14, ma qui la fix "ovvia" romperebbe funzionalità esistente: `entrypoint-wrapper.sh` scrive `/etc/pgbackrest/pgbackrest.conf` e ne fa `chown postgres:postgres` (un processo non-root non può cambiare proprietario a un altro utente), poi ogni comando verso Postgres/pgbackrest passa già per `gosu postgres ...` — esattamente il pattern privilege-drop dell'immagine ufficiale `postgres:15-alpine` upstream (il suo stesso `docker-entrypoint.sh`, quando parte come root, fa `gosu postgres` internamente prima di avviare il server; Postgres stesso rifiuta di girare come root). A differenza di nginx (GAP-14), non esiste un'immagine "unprivileged" equivalente: pgBackRest richiede root transitorio per sistemare i permessi di un volume montato al primo avvio. **Nessuna modifica di codice** — rischio residuo accettato, stesso trattamento di T-OPS-02 | docker/postgres/Dockerfile | — | A05 | ⚠️ RISCHIO ACCETTATO |
| T-AUTH-11 | Rimosso l'endpoint pubblico `POST /api/v1/auth/register` (mai raggiungibile da UI, dead surface): `AuthController.register()`, `AuthService.register()`/`AuthServiceImpl.register()`, `RegisterRequest.java`, `UserAccountMapper.java` eliminati; rimosso dall'allowlist CSRF del gateway. L'unico path di creazione account restante (`UserManagementController.createUser`) resta dietro `@PreAuthorize hasAnyRole('ADMIN','OWNER')` e deriva `hotelId` dall'header `X-Auth-Hotel` iniettato dal gateway, mai dal body client. **Nota residua non-exploitable**: `SecurityConfig.java` di auth-service elenca ancora `/api/v1/auth/register` in `permitAll()`/`HMAC_EXEMPT_PATH_PREFIXES` — inerte (404, nessun controller la mappa più) ma da ripulire per evitare che un futuro dev vi appenda inavvertitamente un handler pubblico | auth-service/{controller/AuthController.java, service/AuthService.java, service/impl/AuthServiceImpl.java}, rimossi RegisterRequest.java + UserAccountMapper.java | efc0069 | A01 | ✅ |
| T-AUTH-12 | Lockout (T-AUTH-02) esteso da chiave solo-username a coppia `(username, clientIp)`: nuovo `LoginAttemptService`/`LoginAttemptServiceImpl` (Redis, chiavi `login:fail:<user>:<ip>`/`login:lock:<user>:<ip>`); nuovo `ClientIpFilter` (api-gateway, `GlobalFilter` `HIGHEST_PRECEDENCE+3`) rimuove qualunque `X-Client-IP` client-supplied e lo reimposta dal TCP remoteAddress prima di inoltrare ad auth-service (non raggiungibile direttamente dall'esterno in prod). **Regressione trovata in re-audit 2026-08-16**: la protezione collassa a `(username)` per tutto il traffico reale che passa dal path standard browser→nginx→gateway, perché nginx è un reverse-proxy L7 senza PROXY protocol sulla stessa rete Docker — vedi GAP-17 per dettaglio ed exploit. **Fix 2026-08-16 (GAP-17)**: `clientIp` è ora derivato correttamente dietro nginx — vedi GAP-17 per il meccanismo completo; `(username, clientIp)` torna a variare per utente reale dietro il front-door standard | api-gateway/filter/ClientIpFilter.java, auth-service/service/{LoginAttemptService.java, impl/LoginAttemptServiceImpl.java}, AuthController.java, AuthServiceImpl.java | 673d5f1, ff432e3 | A07 | ✅ |
| T-AUTH-13 | `GlobalExceptionHandler.handleAccountLockedException` ora restituisce lo stesso 401/`INVALID_CREDENTIALS` di `handleBadCredentialsException` (corpo byte-identico); `AuthServiceImpl.login()` riscritto per eseguire `passwordEncoder.matches()` incondizionatamente (hash reale o un `dummyHash()` lazy-memoized sullo stesso encoder) prima di qualunque branch che decida quale eccezione lanciare — utente sconosciuto, account bloccato e password errata pagano ora lo stesso costo Argon2id e restituiscono lo stesso status/corpo | auth-service/exception/GlobalExceptionHandler.java, service/impl/AuthServiceImpl.java | 191cf51 | A07 | ✅ |
| T-GW-10 | `RateLimiterConfig.remoteAddrKeyResolver`/`userKeyResolver` non leggono più `X-Forwarded-For` da nessun punto del codice — usano esclusivamente `exchange.getRequest().getRemoteAddress()`. **Regressione trovata in re-audit 2026-08-16**: stessa causa radice di T-AUTH-12 — vedi GAP-17. **Fix 2026-08-16 (GAP-17)**: entrambi i resolver ora leggono l'header `X-Client-IP` iniettato da `ClientIpFilter` invece di ri-derivare l'IP autonomamente — un solo punto di risoluzione dell'IP nel gateway, eliminando la duplicazione che aveva causato questa stessa regressione | api-gateway/config/RateLimiterConfig.java | b5d11a8, ff432e3 | A05 | ✅ |
| GAP-17 | **Trovato durante la re-verifica post-hardening del 2026-08-16** (2 sotto-agenti indipendenti, categorie auth-jwt e access-control, hanno segnalato lo stesso problema separatamente): il fix di T-AUTH-12/T-GW-10 sostituisce la fonte di trust da un header spoofabile (`X-Forwarded-For`) al TCP `remoteAddress` grezzo — corretto in isolamento, ma nella topologia reale il browser non parla mai direttamente con `api-gateway`: passa sempre per `frontend` (nginx), che fa da reverse-proxy L7 (`proxy_pass http://api-gateway:8080`) sulla stessa rete Docker (`dmz-network`), senza PROXY protocol e senza alcuna configurazione di trust sugli header forwarded lato gateway (nessun `server.forward-headers-strategy`/`ForwardedHeaderFilter`). Risultato: ogni richiesta che passa dal front-door standard (`https://host/` → nginx → gateway) arriva al gateway con lo stesso peer TCP — l'IP interno del container nginx, identico per **ogni** utente reale. `ClientIpFilter` inietta quindi lo stesso `X-Client-IP` per tutta la popolazione utenti reale, collassando la chiave di lockout `(username, clientIp)` a `(username)` esattamente come prima del fix; `RateLimiterConfig` colleziona allo stesso modo tutti i client in un unico bucket condiviso (DoS di affidabilità: un client pesante esaurisce il bucket per tutti). **Exploit concreto**: 5 richieste `POST /api/v1/auth/login` con `username=admin` e password errata, dalla normale pagina di login pubblica (nessun bypass richiesto) — blocca `(admin, <ip-interno-nginx>)`, che è la stessa chiave condivisa da ogni utente reale, negando il login del vero admin per 15 minuti, ripetibile indefinitamente. Non catturato dai test unitari esistenti (`ClientIpFilterTest`/`RateLimiterConfigTest` mockano il remoteAddress direttamente, non esercitano mai l'hop reale nginx→gateway). **Nessun fix applicato in questo giro** — richiede: (a) far calcolare a nginx un identificativo client reale e farlo fidare dal gateway solo quando proviene dall'indirizzo interno noto di nginx, oppure (b) chiudere l'esposizione diretta di `api-gateway:8080` in prod e configurare `ForwardedHeaderFilter`/`server.forward-headers-strategy` scoped al solo hop nginx noto. **Fix 2026-08-16 (opzione b)**: `docker-compose.prod.yml` non pubblica più `api-gateway:8080` sull'host (solo `frontend:80` resta raggiungibile — `dmz-network` interno non è affetto dalla rimozione del port publish, verificato con `docker compose config`) e imposta `SPRING_PROFILES_ACTIVE=prod`; nuovo flag `gateway.security.trust-forwarded-header` (`config-service/api-gateway.yml`=false, `api-gateway-prod.yml`=true) fa sì che `ClientIpFilter` si fidi di `X-Real-IP` (nginx lo imposta sempre da `$remote_addr`, overwrite non append) solo quando quel flag è vero — cioè solo quando la porta diretta è davvero chiusa. In dev (porta ancora aperta, flag false) il comportamento resta invariato: TCP remoteAddress, header ignorato, finding #3 resta chiuso anche lì. **Regressione trovata durante la stessa riverifica**: la prima versione del fix impostava `trust-forwarded-header: true` solo in `api-gateway-prod.yml`, ma nessun file del repo settava mai `SPRING_PROFILES_ACTIVE=prod` per `api-gateway` (unico servizio senza, a differenza degli altri 6) — Spring Cloud Config non serve mai quel file senza profilo attivo, quindi il flag restava `false` anche a porta chiusa e GAP-17 restava di fatto aperto. Corretto nello stesso commit. Verificato: `./gradlew :api-gateway:test` verde (nuovi test `ClientIpFilterTest`/`RateLimiterConfigTest` per il path trust-forwarded-header=true, inclusa la simulazione "TCP peer = IP del container nginx"), `docker compose config` conferma merge additivo delle env var e nessuna porta pubblicata per `api-gateway` | api-gateway/{filter/ClientIpFilter.java, config/RateLimiterConfig.java}, config-service/config/{api-gateway.yml, api-gateway-prod.yml}, docker-compose.prod.yml | ff432e3 | A05 | ✅ |
| GAP-18 | **Trovato durante la re-verifica post-hardening del 2026-08-16** (sotto-agente business-logic): `@SQLDelete` su `Quotation` e `RateSeason` (`UPDATE ... SET active = false WHERE id = ?`) non verifica né incrementa la colonna `version` aggiunta da T-QUOT-01/T-PRICE-01 — a differenza di `Reservation`, che nello stesso codebase usa correttamente `UPDATE ... SET active = false, version = version + 1 WHERE id = ? AND version = ?`. Poiché l'`UPDATE` generato da Hibernate per un `save()` normale scrive comunque tutte le colonne mappate incluso `active`, una race UPDATE-vs-DELETE concorrente sullo stesso `Quotation`/`RateSeason` non genera mai un conflitto: un `DELETE` (staff B) può essere silenziosamente annullato da un `save()` concorrente (staff A) che non sa nulla della cancellazione, "resuscitando" un preventivo appena cancellato come `ACCEPTED` con una prenotazione già creata, o riportando in vita una stagione tariffaria che un secondo admin credeva cancellata. Non riapre l'invariante di double-booking (T-RES-01 residuo copre quello a livello DB), ma è lo stesso tipo di lost-update che T-QUOT-01/T-PRICE-01 dovevano chiudere. **Nessun fix applicato in questo giro** — richiede allineare entrambi gli `@SQLDelete` al pattern già in uso su `Reservation.java` (`AND version = ?` + incremento), o rimuovere l'override e lasciare che Hibernate generi il DELETE version-checked di default. **Fix 2026-08-16**: entrambi gli `@SQLDelete` allineati esattamente al pattern di `Reservation` (`SET active = false, version = version + 1 WHERE id = ? AND version = ?`). **Regressione trovata durante la stessa riverifica**: il pattern di `Reservation` replicato non enforcava mai davvero il controllo versione — `@SQLDelete` di Hibernate usa `ResultCheckStyle.NONE` di default, quindi una delete con version disallineata aggiorna 0 righe e Hibernate non se ne accorge, riportando successo pur non avendo cancellato nulla (race inversa: una delete stale sopravvive silenziosamente a un save concorrente che ha vinto per primo). Stesso difetto latente presente anche in `Reservation.java` fin da T-RES-01, non introdotto da questo fix. Aggiunto `check = ResultCheckStyle.COUNT` a tutti e tre gli `@SQLDelete` (Quotation, RateSeason, **e Reservation**) — una delete a 0 righe ora solleva la `StaleStateException` che `GlobalExceptionHandler` già mappa a 409 `CONCURRENT_MODIFICATION`. **Nota di copertura test**: nessun test a livello repository esercita la SQL reale di delete per nessuna delle tre entità (tutti i test di service mockano `repository.delete()`) — il fix è verificato per compilazione/build (`./gradlew :frontdesk-service:test` verde) ma non da un test che dimostri il 409 su una delete stale; da pianificare in un giro successivo. **Chiuso 2026-08-16**: nuovo `SoftDeleteVersionCheckIntegrationTest` (Testcontainers PostgreSQL reale, stesso setup di `RoomTypeServiceIntegrationTest`, nessun mock su `repository.delete()`) — per ciascuna delle tre entità: carica la riga, bump della `version` via SQL nativo per simulare un salvataggio concorrente mai visto dall'istanza in memoria, poi delete con la `version` stale; verifica che sollevi `ObjectOptimisticLockingFailureException`. Verificato che il test cattura davvero la regressione: rimosso temporaneamente `check = ResultCheckStyle.COUNT` dalle tre entità, tutti e 3 i test falliscono (nessuna eccezione, delete stale "riuscita" silenziosamente); ripristinato il fix, tutti e 3 tornano verdi | frontdesk-service/{quotations/domain/Quotation.java, pricing/domain/RateSeason.java, reservations/domain/Reservation.java, src/test/java/com/hotelpms/frontdesk/integration/SoftDeleteVersionCheckIntegrationTest.java} | 5f5a698, 83dfda4 | A04 | ✅ |
| T-PRICE-01 | Vedi §4.4b per il dettaglio del threat | frontdesk-service/pricing/{domain/RateSeason.java, service/impl/RateCalendarServiceImpl.java}, V12__add_version_to_rate_seasons.sql | 5cda2cc | A04 | ✅ |
| T-QUOT-01 | Vedi §4.4b per il dettaglio del threat | frontdesk-service/quotations/{domain/Quotation.java, service/impl/QuotationServiceImpl.java}, V13__add_version_to_quotations.sql | 421d5bd | A04 | ✅ |
| GAP-19 | Redis (backing store del rate limiting gateway, blacklist refresh-token + login lockout di auth-service, e nonce anti-replay condiviso da ogni `InternalAuthFilter` su tutti e 7 i servizi backend) girava senza `requirepass`/ACL — chiunque raggiungesse la rete Docker interna poteva leggere/scrivere lo stato di sessione, invalidare token, o disattivare il rate limiting. Trovato dall'audit a 10 agenti | docker-compose.yml (redis service + 7 servizi consumer), config-service/config/application.yml, .env.example | 1006bac | A05 | ✅ |
| GAP-20 | `AbstractProblemDetailAdvice.handleFeignException` e i 3 handler `ExternalServiceException` per-servizio (frontdesk/fb/billing) restituivano `ex.getMessage()` nel corpo 502 — includeva URL Docker interni (`http://billing-service:8085/...`), utile a un attaccante per mappare la topologia di rete interna. Trovato dall'audit a 10 agenti. Fix: dettaglio loggato solo server-side, corpo client fisso `EXTERNAL_SERVICE_ERROR` | common-web-lib/exception/AbstractProblemDetailAdvice.java, frontdesk/fb/billing-service/exception/GlobalExceptionHandler.java | ac059d8 | A05 | ✅ |
| GAP-21 | Nessun limite superiore su diverse liste di request DTO (`OrderItemRequest.quantity`, `RestaurantOrderRequest.items`, `ReservationRequest.lineItems`, `QuotationOptionRequest.roomIds`, `RateBulkApplyRequest.roomTypeIds`) — leva di esaurimento risorse (payload enormi accettati e processati). Trovato dall'audit a 10 agenti. Fix: `@Max(100)` su quantity, `@Size(max=50)` sulle 4 liste, soglie proposte e motivate prima dell'applicazione | fb-service/dto/{OrderItemRequest.java, RestaurantOrderRequest.java}, frontdesk-service/{reservations/dto/ReservationRequest.java, quotations/dto/QuotationOptionRequest.java, pricing/dto/RateBulkApplyRequest.java} | f1ab269 | A04 | ✅ |
| GAP-22 | 4 file `application-{auth,billing,fb,guest}-service.yml` avevano una password Postgres locale-dev hardcoded in chiaro. Trovato dall'audit a 10 agenti. Fix: resa overridabile via `${POSTGRES_PASSWORD:<valore-locale-esistente>}` in tutti e 4 (non rimossa — `docker-compose.yml` la sovrascrive comunque via `SPRING_DATASOURCE_PASSWORD`, che come env var OS ha precedenza su `application-{profile}.yml`; rimuoverla avrebbe rotto il `bootRun` locale non-Docker) | auth/billing/fb/guest-service/src/main/resources/application-*-service.yml | a2c33eb | A02 | ✅ |
| T-FE-05 | Vedi §4.9 per il dettaglio del threat | frontend/nginx.conf | d8cafcf | A05 | ✅ |
| T-GW-11 | Vedi §4.2 per il dettaglio del threat | api-gateway/events/{EventStreamController.java, RoomEventBroadcaster.java}, config-service/config/api-gateway.yml | [pending] | A01 | ✅ |
| T-GW-12 | Vedi §4.2 per il dettaglio del threat | api-gateway/events/InternalEventsAuthFilter.java, frontdesk-service/{client/GatewayEventsClient.java, config/frontdesk-service.yml} | [pending] | A01 | ✅ |

**Legenda stato**: ⏳ In attesa | 🔄 In corso | ✅ Completato

---

## 7. Mappatura OWASP Top 10 (2021)

| OWASP | Nome | Threats mappati |
|-------|------|----------------|
| A01 | Broken Access Control | T-GST-01, T-GST-03, T-GST-07, T-RES-02, T-BILL-01, T-BILL-04, T-BILL-07, T-FB-01, T-FE-03, T-GW-05, T-GW-06, T-GW-07, T-GW-08, T-GW-09, T-GW-11, T-GW-12, T-STAY-04, T-STAY-05, T-STAY-06, T-ROOM-01, T-ROOM-02, T-AUTH-06, T-AUTH-10, T-AUTH-11, T-NOTIF-01, GAP-3, GAP-7, GAP-9 |
| A02 | Cryptographic Failures | T-AUTH-03, T-CFG-02, T-STAY-03, GAP-22 |
| A03 | Injection | T-GST-02, T-RES-03, T-FE-01 |
| A04 | Insecure Design | T-RES-01, T-RES-01-residuo, T-STAY-01, T-STAY-07, T-FB-02, T-BILL-02, T-BILL-05, T-GST-04, T-GST-05, T-GST-06, T-AUTH-07, T-PRICE-01, T-QUOT-01, GAP-7, GAP-18, GAP-21 |
| A05 | Security Misconfiguration | T-GW-02, T-GW-03, T-GW-04, T-GW-10, T-CFG-01, T-CFG-03, T-FE-04, T-FE-05, GAP-10, GAP-11, GAP-12, GAP-13, GAP-14, GAP-16, GAP-17, GAP-19, GAP-20 |
| A06 | Vulnerable and Outdated Components | DEP-CVE-01, DEP-CVE-02, DEP-CVE-03, DEP-CVE-04, DEP-CVE-05, DEP-CVE-06, DEP-CVE-07 |
| A07 | Identification & Authentication Failures | T-AUTH-01, T-AUTH-02, T-AUTH-03, T-AUTH-04, T-AUTH-04-residuo, T-AUTH-06, T-AUTH-08, T-AUTH-09, T-AUTH-12, T-AUTH-13, T-FE-02, T-NOTIF-01 |
| A08 | Software and Data Integrity Failures | GAP-15 |
| A09 | Security Logging & Monitoring Failures | T-AUTH-05, T-STAY-02, T-BILL-03, GAP-4 |
