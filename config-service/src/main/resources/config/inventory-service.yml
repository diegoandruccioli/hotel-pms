server:
  port: 8081

spring:
  data:
    redis:
      host: "${REDIS_HOST:redis}"
      port: 6379

internal:
  hmac:
    secret: ${INTERNAL_HMAC_SECRET}

management:
  endpoints:
    web:
      exposure:
        include: "prometheus,health,info"
  endpoint:
    health:
      probes:
        enabled: true
      show-details: always
  health:
    livenessstate:
      enabled: true
    readinessstate:
      enabled: true
  tracing:
    sampling:
      probability: 1.0
