<?xml version="1.0" encoding="UTF-8"?>
<suppressions xmlns="https://jeremylong.github.io/DependencyCheck/dependency-suppression.1.3.xsd">
    <!--
        OWASP Dependency Check — False Positive Suppressions
        =====================================================
        Add entries here for CVEs that are false positives
        for this project. Each suppression must include a
        justification note explaining why it is safe to ignore.

        Example:
        <suppress>
            <notes><![CDATA[
                This CVE applies to Spring Framework < 6.1.0.
                We use Spring Boot 3.4.x (Framework 6.2.x), which is not affected.
            ]]></notes>
            <cve>CVE-2024-XXXXX</cve>
        </suppress>
    -->
</suppressions>
