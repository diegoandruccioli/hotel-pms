/**
 * Read-only aggregation over rooms, reservations and stays for the
 * front-desk day-sheet view — one endpoint replacing the five separate,
 * unpaginated calls (up to {@code size=500} each) the Dashboard used to make
 * and then aggregate client-side.
 */
package com.hotelpms.frontdesk.dashboard;
