spring:
  application:
    name: frontdesk-service
  config:
    import: "optional:configserver:"
  cloud:
    config:
      enabled: false

internal:
  hmac:
    secret: test-hmac-secret-minimum-32-characters-for-unit-tests
